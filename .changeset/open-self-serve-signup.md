---
"@workspace/selena-visibility-contracts": patch
"@workspace/web": patch
---

Added `SELENA_SELF_SERVE_SIGNUP_ENABLED`, which opens registration without moving the deployment to cloud mode; each new account is provisioned its own workspace.
