---
"@workspace/web": patch
---

The workspace is set like a document: its own typeface finally loads, prose runs at a readable measure in the serif, the tools row reads as a contents line, each measured surface is a row of a table rather than a card of pills, and the overview panel is set in charcoal.
