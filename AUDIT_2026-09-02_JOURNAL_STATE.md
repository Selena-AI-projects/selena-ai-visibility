# Аудит состояния на 2 сентября 2026, 23:30 по Бали (15:30 UTC)

Проверено по фактам: git, GitHub PR, логи Railway, схема базы. Всё, что ниже,
подтверждено чем-то конкретным — коммитом, номером деплоя или строкой лога.
Где не подтверждено, так и написано.

## Короткий ответ

Замер AVLI Bali **по-прежнему не сделан**. Ни одного платного ответа не получено,
ни одного цента 2 сентября не потрачено. Причина не в том, что мы не нашли ошибок —
их нашли и починили пять штук за сутки. Причина в том, что после последнего сбоя
заявка проекта осталась в состоянии `HOLD`, а из `HOLD` в текущем коде выхода нет
ни у кого. Это состояние сейчас — единственное, что стоит между нами и замером.

## Что сделано и подтверждено

| Что | Подтверждение | Состояние |
| --- | --- | --- |
| Расхождение истории миграций staging и репозитория | PR #109, деплой `a44679d7` | закрыто |
| Контейнеры перестали стартовать через `npx` (сеть в момент запуска) | PR #110 | закрыто |
| Освобождение зависшей заявки сравнивало время с округлением до миллисекунд — не совпадало никогда | PR #111 | закрыто |
| Гонка на времени: `CURRENT_TIMESTAMP` при шести параллельных работниках отбрасывал время назад | воспроизведено локально; в релизе везде `clock_timestamp()` | закрыто |
| Миграции `0054`–`0058` применены на staging | деплой `b0d35f0d`, лог `journal before: 59 / journal after: 59` | закрыто |
| Крупная работа v1.3: RLS, доказательства, навигация HoReCa, вход на сайте | PR #96, #114, #115, #117 — все смержены | закрыто |
| Замер не может стартовать «сам по себе» | новый шлюз `measurement-deployment-gate.ts` | работает, см. ниже |

Расход 2 сентября по биллингу Bright Data — **$0**. Единственная запись за день
не наша (YouTube-снимок в 06:37 UTC). За 1 сентября — $0.0015 за один ответ
Google AI Mode. Это проверено сплошной сверкой всех 4 980 снимков аккаунта.

## Что мешает прямо сейчас

### 1. Заявка AVLI Bali в состоянии `HOLD` — выхода нет

Заявка `af061339-26dd-4862-91af-9a6295049047`, лок `22214dbe-…`, состояние `HOLD`
с 02:23 UTC 2 сентября. При ней: 75 разрешений, 4 израсходованы, 4 запуска висят
в `RUNNING`, событий стоимости — ноль.

В миграции `0058` появилась функция восстановления `sv_recover_journal_daily_claim`.
Она умеет освобождать заявку, но только из состояния `CLAIMED` и только при
доказанном отсутствии трат. Для заявки, которая уже в `HOLD`, она сразу
возвращает `HOLD` и ничего не делает. Триггер по-прежнему запрещает любой переход
из `HOLD`, удаление строки запрещено, а частичный уникальный индекс не даёт
создать новую заявку для проекта.

Итог: **проект AVLI Bali сейчас неизмерим в принципе**, пока не появится
отдельный, отрецензированный способ выхода.

### 2. Способ выхода написан, но нигде не сохранён

OpenCode сделал именно это — owner-only функцию восстановления `HOLD → NO_SPEND`
с неизменяемым сертификатом и запретом поздних записей, плюс шесть правок
безопасности времени выполнения (остановка пула после первой ошибки, обновление
аренды до расходования разрешения, отказ переписывать успешный ответ провайдера
как синтетическую ошибку и другие).

**Всё это лежит некоммиченным на его машине**, в отдельной рабочей копии, и нигде
не запушено. Если та сессия закроется — работа пропадёт. Это сейчас самый
дорогой незакрытый риск, дороже самого `HOLD`.

Кроме того, он нумеровал миграцию как `0057`, а в репозитории уже заняты и `0057`,
и `0058`. При переносе её надо перенумеровать в `0059`.

### 3. Даже после освобождения замер не стартует: нет одобрения деплоя

В PR #96 появился новый шлюз. Замер запускается, только если совпадают:

- `SELENA_MEASUREMENT_APPROVED_COMMIT_SHA` — точный 40-значный коммит деплоя;
- `SELENA_MEASUREMENT_APPROVED_ENVIRONMENT` — имя окружения (`staging`).

**Обеих переменных на сервисе `measure` сейчас нет.** Последний деплой
(`c148959d`, 15:13 UTC) отработал ровно одну строку: `JOURNAL_MEASUREMENT_DEPLOYMENT_NOT_APPROVED`.
Это правильное поведение — так замер больше не может запуститься случайно от
любого мерджа, — но это ещё одно действие владелицы перед запуском.

### 4. Прямой вход в базу под владельцем сломан

В PR #117 записано открытым текстом: после смены пароля прямой логин `postgres`
не проходит; рабочая роль `selena_app` жива, но обслуживание под владельцем
заблокировано (`HOLD_OWNER_DB_BINDING`).

Это связано с пунктом 2 напрямую: восстановление из `HOLD` по замыслу выполняет
**владелец напрямую в базе**. Пока вход под владельцем не восстановлен, выполнить
восстановление будет некому — даже когда код будет готов.

## Что осталось от моей работы

PR [#112](https://github.com/parkourcafe/selena-ai-visibility/pull/112) —
**устарел, мерджить нельзя**. Причины:

- его миграция называется `0056`, а этот номер в репозитории уже занят другой
  миграцией (`0056_formal_evidence_acceptance_hardening`);
- ветка конфликтует с релизом (`mergeable_state: dirty`);
- его правило освобождения — «нет ни одной строки в `sv_runs`» — для нашей заявки
  всё равно не сработало бы: строк там четыре. Подход `0058` (доказательство через
  журнал пересечения границы провайдера) строже и правильнее.

Одна часть #112 при этом в релиз не попала и остаётся полезной: **печать причины
ошибки базы**. Сейчас отказ базы виден как `Failed query: ...` без имени
ограничения, которое отказало. Именно из-за этого утром я потерял целый круг
диагностики. Это стоит перенести отдельным маленьким PR.

## Открытые PR

| PR | Что | Состояние |
| --- | --- | --- |
| #106 | historical reconciliation replay | открыт с 1 сентября, не двигался |
| #108 | увеличение таймаута E2E в CI | открыт с 1 сентября |
| #112 | мой hold release | устарел, надо закрыть |
| #116 | сохранение проекта HoReCa при переходах | черновик |

## Незакрытые дефекты

1. **Внутренний учёт завышает расход примерно в семь раз.** В
   `packages/lib/src/usage/cost.ts` до сих пор заглушка `brightdata: 0.01` при
   реальной цене ответа $0.0015. Проверено по текущему релизу.
2. **Perplexity не появляется в биллинге Bright Data**, хотя Google AI Mode
   появляется. Не выяснено, не тарифицируется он или не доходит.
3. **Четыре запуска висят в `RUNNING`** с 02:23 UTC. Их надо либо примирить, либо
   огородить — иначе они будут мешать любой следующей проверке доказательств.
4. **Пропускная способность.** Очередь последовательная: десять клиентов
   одновременно получат результат через часы, а не минуты.
5. **Better Auth** делит один лимит запросов на всех, потому что не разобран
   доверенный заголовок IP от Railway (записано в #117).

## Порядок действий, чтобы получить замер

1. **Сохранить работу OpenCode.** Пусть закоммитит и запушит ветку с owner-only
   восстановлением и правками безопасности, перенумеровав миграцию в `0059`.
   Это первое и самое срочное — работа сейчас существует в одном экземпляре.
2. **Восстановить вход в базу под владельцем.** Без этого восстановление из
   `HOLD` выполнить некому.
3. **Отрецензировать и смержить** ветку восстановления, применить `0059` на
   staging тем же порядком, что и раньше (переменная потолка + `Deploy` в баннере
   отложенных изменений).
4. **Выполнить восстановление** заявки AVLI Bali — действие владелицы, напрямую
   в базе, с сертификатом.
5. **Выставить одобрение деплоя** на сервисе `measure`: два значения из пункта 3
   раздела «Что мешает», и нажать `Deploy`.
6. **Запустить замер.** 75 ответов, примерно час, реальный расход около 11 центов.
7. После успеха вернуть `SELENA_JOURNAL_FORCE` в `0`.

## Чего я не знаю

- Точное текущее содержимое строки заявки в базе — читал её не я, а OpenCode;
  прямого доступа к staging у меня нет.
- Жива ли ещё сессия OpenCode и цела ли его рабочая копия.
- Значения переменных Railway: агенту они отдаются только по именам, без значений.
