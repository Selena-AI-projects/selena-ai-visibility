# Передача в новый чат — 31 августа 2026, вечер

Читать первым. Здесь всё, что нужно, чтобы продолжить с той же точки.

## Правила работы с владельцем

Selena — основатель, русскоязычная, не технический специалист, работает голосом, Бали (UTC+8).
Каждый ответ начинается с даты и времени Бали: `TZ=Asia/Makassar date`.
Короткие абзацы, простой язык, без жаргона. Она читает с телефона.
В интерфейсы (Railway, Vercel) кликает сама — давать точные пошаговые инструкции.

## Что горит прямо сейчас

**Замер журнала (`measure`) падает на первой секунде. Причина найдена и подтверждена.**

Лог деплоя `ada58443` (31.08, 15:21 UTC):

```
Measuring 1 project(s) through brightdata-chatgpt, brightdata-gemini, brightdata-perplexity
avlibali: Failed query: select "attempt", "status", "utc_day"
          from "sv_journal_daily_claims" where ...
```

Таблицы `sv_journal_daily_claims` в базе staging нет.

Её создаёт миграция `packages/lib/src/db/migrations/0046_selena_journal_daily_claims.sql`. Миграции **0044–0051 все добавлены 31 августа** коммитом `0e00df4` (PR #92) и **ни одна не применена к базе staging**: у сервиса `migrate` в этом окружении ноль деплоев и вообще нет конфигурации.

То есть код научился спрашивать новые таблицы, а базе про них не сказали.

### Что нужно от владельца (миграции без её явной команды не запускать)

1. Railway → проект `selena-ai-visibility` → окружение **staging** → сервис **migrate**:
   задать Source branch `release/selena-visibility-mvp` и переменную `DATABASE_URL`.
2. Задеплоить `migrate` — он выполнит `npx drizzle-kit migrate` и остановится.
3. Задеплоить `measure` заново.

Перед этим стоит прочитать все восемь миграций и сказать владельцу, что каждая делает и есть ли риск. Откат — `SELENA_ROLLBACK_RUNBOOK.md`.

### Что уже исправлено сегодня владельцем по моей подсказке

- Сервис `measure` смотрел на ветку `claude/perplexity-integration-recovery-vcpxo6` (полностью позади релиза, без PR #93/#94/#95). Переключён на `release/selena-visibility-mvp`. Именно это вытащило настоящую ошибку наружу.
- На `measure` были две переменные `SELENA_MEASUREMENT_ENABLED`, одна с пробелом на конце. Убрана.

### Чего я НЕ знаю

Владелец говорит, что замер не работает двое суток. Сегодняшнюю поломку я подтвердил логами. Была ли причина той же вчера — не проверял: тогда сервис собирался с другой ветки, где этого кода не было.

## Ключевые факты об инфраструктуре

| Что | Где |
|---|---|
| Проект Railway | `51dd0770-e622-4734-a705-ace401234bb8` |
| Окружение staging | `90f3bf7f-5e53-4de3-a3f7-56052b706f24` |
| Сервисы staging | `web` `55909c04…`, `worker` `a43c94e7…`, `measure` `b61d97b0…`, `publish` `b287d537…`, `migrate` `cfedff9a…`, Postgres |
| production | только базы, приложения нет |
| Релизная ветка приложения | `release/selena-visibility-mvp`, сейчас `7ac37f4` |
| Сайт | `parkourcafe/SELENA-AI-COMPANY`, ветка `main`, Vercel |

Доступ через Railway MCP: чтение работает (логи, конфиги, деплои), **запись переменных заблокирована** классификатором — переменные ставит владелец руками.

## Решения владельца, принятые 31 августа

Всё записано в `SELENA_OWNER_DECISION_SEPTEMBER_2026_V1.0.md` (это артефакт `D-00` сентябрьского ТЗ).

| ID | Решение |
|---|---|
| OD-A | Карты, Отзывы, соцсети остаются в продукте; карточка публикуется после того, как источник заработал. Карты, Профиль и Отзывы — только в $79 |
| OD-B | Тесты новых источников: $10 в день всего, $2 в день на источник |
| OD-C | Палитра: тёплый навy — `#12283F` заголовки, `#0D6E85` доказательства, медь `#8F5C34`, бумага `#F6F1E9`; шрифты Cormorant Garamond + Commissioner **не меняются**. Перекрашивать сайт и кабинет вместе |
| OD-D | `SELENA_SUGGEST_BUDGET_USD` = 20, выставлено на `web` и `worker` |
| OD-E | Expert Verified = 800 ответов, не 2000 |
| OD-F | Хранение: сырьё 100 дней, отзывы и соцсети 30 дней, готовые цифры пока клиент с нами |
| OD-G | Профиль и Отзывы уезжают в $79 |
| — | Сентябрьский план утверждён целиком: «работаем по этому плану» |

## Что осталось сделать

1. **Миграции** — см. выше, это первое.
2. **PR на сайте не влит:** [SELENA-AI-COMPANY#113](https://github.com/parkourcafe/SELENA-AI-COMPANY/pull/113) — замена 2000 на 800 в шести местах. Типы чистые, 284 теста проходят. Влить может только владелец.
3. **Правки в документах под решения:** §14 Website TZ (палитра), §8 Website TZ (Карты и Отзывы в $79), `DESIGN.md` приложения (там написано, что синие тона вне бренда — после OD-C неверно).
4. **Незакрытое решение владельца:** OD-07, платная канарейка по Картам — держать закрытой до отдельного решения.

## Документы, написанные сегодня

| Файл | Что это |
|---|---|
| `docs/site-tz/SELENA_SYSTEMS_WEBSITE_ARCHITECTURE_CONTENT_TZ_V1.0.md` | ТЗ сайта, hospitality-first, + оригинал `.docx` |
| `docs/site-tz/SELENA_SYSTEMS_SEPTEMBER_2026_MASTER_EXECUTION_TZ_V1.0.md` | сентябрьский мастер-план, + оригинал `.docx` |
| `SELENA_SYSTEMS_WEBSITE_TZ_V1.0_CLAUDE_REVIEW.md` | проверка ТЗ сайта: 5 блокеров |
| `SELENA_SEPTEMBER_2026_MASTER_TZ_CLAUDE_REVIEW.md` | проверка мастер-плана: 6 поправок |
| `SELENA_SYSTEMS_SITE_CURRENT_STATE_INVENTORY.md` | инвентаризация текущего сайта (роль W0) |
| `SELENA_OWNER_DECISION_SEPTEMBER_2026_V1.0.md` | решения владельца |

Ветка: `claude/selena-systems-tz-sycv1t`, всё запушено.
