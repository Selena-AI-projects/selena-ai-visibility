# Selena AI Visibility v1.2.1 — acceptance matrix

| ID | Requirement | Evidence | Status | Gate |
|---|---|---|---|---|
| GRID-01 | Versioned spherical grid `sv-grid-sphere-v1` | `packages/selena-visibility-contracts/src/visibility-os.ts` | `VERIFIED` | Local |
| GRID-02 | 3×3/5×5, 3000 m corner radius, NW row-major | Contract tests | `VERIFIED` | Local |
| GRID-03 | Canonical antimeridian, fixed scale, UUIDv5 | Regression/golden tests | `VERIFIED` | Local |
| DOM-01 | Replace `LOCAL` with `LOCAL_MAPS`; add `LOCAL_AI` | `0045` source-only atomic backfill, strict checks and postconditions statically reviewed; not applied | `PARTIAL` | Disposable DB |
| LOCK-01 | Immutable versioned child Locks | `0045` source-only unique positive project versions, append-only update/delete/truncate guards and tenant-safe provenance; transactional allocator tests pass; not applied | `PARTIAL` | Disposable DB |
| ATT-01 | Durable attempt ledger, at most 3 attempts | `sv_measurement_attempts` schema, state guards and sequence controls statically verified; not applied | `PARTIAL` | Disposable DB |
| ATT-02 | Ambiguous call reconciliation without duplicate spend | Same-attempt reclaim plus source-only runner: provider call follows committed `SUBMITTED` once; ambiguous paths never recall it; durable fencing/result persistence prerequisite is source-only and aggregate store/reconciliation remains absent | `PARTIAL` | Runtime store |
| BUD-01 | Atomic reservation/spend/release | Non-authoritative aggregate projection and exact runner postconditions verified: UNKNOWN/estimated-zero→RESERVED, positive known→SPENT, actual known zero→RELEASED; aggregate cap transaction absent | `PARTIAL` | Runtime/migration draft |
| RLS-01 | Non-owner runtime role and transaction-local tenant context | Attempt RLS and tenant-safe FKs drafted; runtime role/GUC proof absent | `PARTIAL` | Disposable DB, then shared staging |
| MAP-01 | Deterministic free Maps stub | 125-slot `REHEARSAL_ONLY` adapter; SHA-bound frozen request; zero network/cost; synthetic output is non-persistable and evidence-ineligible | `VERIFIED` | Local |
| MAP-02 | Source-only live Maps runner boundary | Store-only READY gate, runner-owned result envelope, frozen-window checks, branded scoped continuation, exact finalize proof; 21 unit tests and three blind reviews pass; no adapter/export/worker registration | `VERIFIED` | Local protocol only |
| MAP-03 | Durable live-result persistence prerequisite | 0044 source-only: digest-bound immutable submitted candidate, unique token digest, trigger-owned row version, exact tenant/cycle/provider/attempt/result identity, strict union/evidence/cost binding and append-only result; 25 schema tests plus two final blind reviews pass; future store must supply CAS | `VERIFIED` | Local static/schema proof only; not applied |
| HARD-01 | Remove stale `LOCAL`, harden Configuration Lock/cost ledger and factual UI copy | Source-only `0045`, schema, Gate12, allocator and UI remediation implemented; evidence→dataset/source and cost→cycle provenance are tenant-safe; row-locking guards close parent/child scope races; static tests and two blind reviews pass; no migration applied | `PARTIAL` | Disposable DB |
| OPS-01 | Prevent duplicate paid journal runs after concurrency or restart | `0046` source-only DB-clock identity/timestamps, project-wide unresolved uniqueness across days/versions, transactional advisory allocation, any-same-day-completion guard, claim↔Lock provenance and audited allocation/state machine; only proven pre-provider errors become `NO_SPEND`, while ambiguous/partial paths block repeats; targeted tests and blind review pass | `PARTIAL` | Disposable DB; no paid run |
| API-01 | Local quote/create/progress/results/evidence APIs | `apps/web/src/server/selena-local-read-api.ts`, four tenant-scoped GET routes, lock-first quote/create transport scaffolding, contracts, canonical Local Maps CSV projection, tests and `packages/api-spec/src/openapi.json`; progress/results/evidence are fail-closed, Maps provenance requires `MAPS_SERP_PROVIDER`, and Local AI is MANUAL_ONLY with locked-context/coordinate-proof checks | `PARTIAL` — read-only progress/maps/results/evidence plus source-only quote/create handlers are implemented and source-tested; default mutation store returns `503 OWNER_GATE_REQUIRED` and never fabricates `201`; canonical CSV is bounded and excludes private/raw references. Two fresh blind reviews found no P0 and confirmed tenant/scope fencing, but durable idempotency/persistence, tamper-evident cursors, complete asset high-water snapshot, real DB/RLS/runtime proof and signed evidence access remain absent or owner-gated | Local source/runtime |
| PAY-01 | One-task paid canary | No approval and prerequisites incomplete | `NOT_STARTED` | Owner paid gate |
| PROD-01 | Checkout, recurring scans, production acceptance | Explicitly disabled | `NOT_STARTED` | Owner production gates |
