# Selena AI Visibility v1.2.1 — acceptance matrix

| ID | Requirement | Evidence | Status | Gate |
|---|---|---|---|---|
| GRID-01 | Versioned spherical grid `sv-grid-sphere-v1` | `packages/selena-visibility-contracts/src/visibility-os.ts` | `VERIFIED` | Local |
| GRID-02 | 3×3/5×5, 3000 m corner radius, NW row-major | Contract tests | `VERIFIED` | Local |
| GRID-03 | Canonical antimeridian, fixed scale, UUIDv5 | Regression/golden tests plus explicit decimal ROUND_HALF_UP and rounded-center identity alignment | `VERIFIED` | Local |
| GRID-04 | Legacy planar grid is quarantined from normative Local Maps | Historical `squareGridPoints` compatibility generator is isolated in `legacy-visibility-grid.ts` and marked deprecated; normative Local Maps callers use `sphericalGridPointsV1` | `VERIFIED` | Local source only |
| DOM-01 | Replace `LOCAL` with `LOCAL_MAPS`; add `LOCAL_AI` | `0045` atomic backfill, strict checks and postconditions passed in dedicated disposable PostgreSQL rehearsal; shared environments remain untouched | `VERIFIED` | Disposable DB |
| LOCK-01 | Immutable versioned child Locks | `0045` source-only unique positive project versions, append-only update/delete/truncate guards and tenant-safe provenance; transactional allocator tests pass. Completion audit found that the Maps Lock freezes keyword IDs/version but candidate construction reads mutable current keyword text; not applied | `PARTIAL` — keyword request snapshot/version conflict remains | Owner contract decision, then disposable DB |
| ATT-01 | Durable attempt ledger, at most 3 attempts | `sv_measurement_attempts` schema, state guards and sequence controls plus `0047` Local Maps observation cap statically verified; shared seven-part execution-key parser is used by generation and budget projection. Forward-only `0049` repair and a disposable lifecycle runner are prepared; the isolated Gate12 rehearsal applied the chain, but the dedicated row-level transition remains unexecuted | `PARTIAL` — runtime lifecycle proof pending | Fresh owner-authorized disposable DB run |
| ATT-02 | Ambiguous call reconciliation without duplicate spend | Same-attempt reclaim plus source-only runner: provider call follows committed `SUBMITTED` once; concrete source-only store now performs token/rowVersion fencing, append-only result persistence and no-recall UNKNOWN paths; durable snapshot query, runtime RLS and aggregate reconciliation remain absent | `PARTIAL` | Runtime store |
| BUD-01 | Atomic reservation/spend/release | Source-only store and pure tests enforce UNKNOWN→RESERVED, positive known→SPENT, actual known zero→RELEASED with exact micro-unit arithmetic and append-only cost event ordering; authoritative aggregate budget callback/cap transaction remains owner-gated | `PARTIAL` | Runtime/migration draft |
| RLS-01 | Non-owner runtime role and transaction-local tenant context | Attempt RLS and tenant-safe FKs drafted; runtime role/GUC proof absent | `PARTIAL` | Disposable DB, then shared staging |
| MAP-01 | Deterministic free Maps stub | 125-slot `REHEARSAL_ONLY` adapter; SHA-bound frozen request; zero network/cost; synthetic output is non-persistable and evidence-ineligible | `VERIFIED` | Local |
| MAP-02 | Source-only live Maps runner boundary | Store-only READY gate, runner-owned result envelope, frozen-window checks, branded scoped continuation, exact finalize proof and fail-closed validation of the finalization discriminant; 24 focused runner tests and three blind reviews pass; no provider/worker registration | `VERIFIED` | Local protocol only |
| MAP-04 | Normative `LocalMapsRankAdapter` boundary | `local-maps-rank-adapter.ts` defines lock-derived quote, committed permit, exact coordinate/request echo proof and normalize contract; task/proof requests are schema-validated and compared in canonical field order, while the runner bridge rejects missing/altered proof before persistence and remains unregistered/fail-closed. Reordered-key and malformed-task regressions are covered | `VERIFIED` | Local source only; provider activation owner-gated |
| MAP-05 | Reviewed Maps identity fallback is explicit and auditable | Setup and Lock contracts preserve primary Place ID/CID compatibility and require matched name/address plus `REVIEWED_NAME_ADDRESS_FALLBACK`, `REVIEWED_MATCH` and `reviewed=true` when no primary identifier exists; unresolved identities fail closed; OpenAPI request/response parity test covers both branches | `VERIFIED` | Local source only |
| MAP-06 | Live Maps evidence has an auditable check reference | Live `FOUND`/`ABSENT` provenance requires a non-empty, non-stub `checkReference`; provider-auth/failure/unknown outcomes may retain a nullable reference. Contract, fixture, migration-0044 static shape and Drizzle checks align; no migration or provider execution was performed | `VERIFIED` | Local source/static schema only |
| MAP-07 | Maps metric completeness: competitor wins, distance-rank curve and invalid-point rate | `competitorWinCoverage`, `localDistanceRankCurve` and `invalidPointRate` implement §8.6 formulas as pure functions. Distance bands are caller-supplied, ordered, non-overlapping half-open intervals; valid/absent/invalid denominator rules remain explicit. Focused and full contracts tests pass `232/232`; no runtime persistence or provider execution is implied | `VERIFIED` | Local source only; runtime aggregation remains owner-gated |
| MAP-08 | §8.5 evidence contract is complete and durable | Crosswalk audit confirms `scan_cycle_id`, point/coordinates, keyword, provider, capture time and raw reference are distributed across `local-maps-live.ts`, attempts, observations and map views. Direct durable fields/links for grid ID in the live envelope, device/OS/language binding, endpoint version, rank group/absolute semantics, check reference, competitors and `cost_event_id` are not all present on the observation/read model; no canonical cross-layer contract exists yet | `PARTIAL` — requires owner-approved canonical evidence shape and migration/runtime proof; no fields were invented | Owner contract decision, then migration/runtime |
| ECON-01 | Historical M0 economics cannot override Delta v1.2.1 | `local-cycle-economics.md` and its generator are explicitly `LEGACY M0 / NON-NORMATIVE`; 7×7/variable-step/one-retry examples remain historical only | `VERIFIED` | Local documentation boundary |
| MAP-03 | Durable live-result persistence prerequisite | 0044 source-only: digest-bound immutable submitted candidate, unique token digest, trigger-owned row version, exact tenant/cycle/provider/attempt/result identity, strict union/evidence/cost binding and append-only result; concrete source-only store supplies CAS, token fencing, cost-event-before-result ordering and immutable result mapping; 9 attempt-store tests, 25 schema tests and three final blind reviews pass; migration/runtime still not applied | `VERIFIED` | Local static/schema proof only; not applied |
| HARD-01 | Remove stale `LOCAL`, harden Configuration Lock/cost ledger and factual UI copy | Source-only `0045`, schema, Gate12, allocator and UI remediation implemented; dedicated disposable PostgreSQL hardening rehearsal passed positive/negative scope and collision fixtures with atomic failure checks; shared environments remain untouched | `VERIFIED` | Disposable DB |
| OPS-01 | Prevent duplicate paid journal runs after concurrency or restart | `0046` source-only DB-clock identity/timestamps, project-wide unresolved uniqueness across days/versions, transactional advisory allocation, any-same-day-completion guard, claim↔Lock provenance and audited allocation/state machine; only proven pre-provider errors become `NO_SPEND`, while ambiguous/partial paths block repeats; targeted tests and blind review pass | `PARTIAL` | Disposable DB; no paid run |
| IDEM-01 | Durable idempotency for mutating Local API requests | Typed seven-day replay/conflict contract, tenant-scoped immutable `sv_api_idempotency_records` schema, RLS/purge guards, migration `0048`, transaction-only persistence helper, and shared route seam exist. No concrete runner claims ownership before business execution, so a naive read→execute→save adapter would allow concurrent duplicate mutations; migration/runtime race proof not run | `PARTIAL` | Transaction design + disposable DB + runtime store |
| API-01 | Local quote/create/progress/results/evidence APIs | `apps/web/src/server/selena-local-read-api.ts`, four tenant-scoped GET routes, source-only location/place-entity/keyword-set setup routes, lock-first quote/create transport scaffolding, six fail-closed admin control routes plus provider capabilities read boundary, contracts, canonical Local Maps CSV projection, typed setup/admin success response contracts, explicit authenticated `tenantId` adapter fields, `LocalMapsRankAdapter` runner bridge, tests and `packages/api-spec/src/openapi.json`; progress/results/evidence are fail-closed, Maps provenance requires `MAPS_SERP_PROVIDER`, and Local AI is MANUAL_ONLY with locked-context/coordinate-proof checks | `PARTIAL` — read-only progress/maps/results/evidence plus source-only location onboarding, place identity confirmation, keyword-set versioning, quote/create, admin preflight/approve/stop/retry/canary and provider-capabilities transport are implemented and source-tested; injected durable adapters can return validated operation-specific 200/201/202 responses, while default mutation/read stores return `503 OWNER_GATE_REQUIRED` and never fabricate success; canonical CSV is bounded and excludes private/raw references. Evidence pagination anchors its snapshot to a full-set server-created `createdAt` high-water query before page slicing, while Local AI result snapshots include attached evidence-asset creation timestamps, so caller-controlled future capture times cannot freeze mutation detection. Local Maps count/results joins explicitly fence `domain_id=LOCAL_MAPS`; cross-tenant evidence-index uniqueness still needs a migration/runtime proof. Quote validation enforces non-empty surfaces plus task/attempt cardinality invariants. The OpenAPI cursor pattern now accepts both unsigned and signed `base64url.signature` tokens; an injectable HMAC cursor capability is covered and route-integrated when an owner-managed secret is supplied, but the default remains unsigned. Explicit tenantId is now required at write/setup/admin adapter boundaries as defense-in-depth; this is not runtime RLS proof. Durable idempotency now has a typed seven-day replay/conflict contract, transaction-only lib persistence boundary, and a shared route runner seam wired into all mutating Local API handlers; the concrete transaction adapter, migration/runtime proof and replay race evidence remain absent. The normative `LocalMapsRankAdapter` contract and runner bridge are source-tested but deliberately unregistered. Earlier independent blind reviews found no P0 in the reviewed slices and confirmed tenant/scope fencing; owner-managed cursor/evidence activation, runtime snapshot/RLS proof and signed evidence access remain absent or owner-gated | Local source/runtime |
| EXPORT-01 | Bounded Local Maps CSV export | `apps/web/src/routes/api/v1/selena/local-scan-cycles/$cycleId/map-results/export.ts`, `createSelenaLocalReadApi.mapResultsCsv`, `serializeLocalMapsCsv`, OpenAPI path and focused route tests | `PARTIAL` — route is tenant/scope-fenced, cursor-stable, capped at 10,000 rows and excludes private/raw references; signed evidence URLs, durable runtime store proof and production reachability remain unverified | Local source only |
| API-01-ERR | Local API client-safe error boundary | `apps/web/src/lib/selena-api-http.ts`, focused HTTP/route tests | `VERIFIED` — both direct and helper error responders apply the same redaction boundary; unknown messages fall back to a generic client-safe message; details are restricted to bounded diagnostic keys and validated values; provider secrets/raw bodies are covered by negative tests. Owner-gate 503s use `retryable:false` so an owner decision is not presented as a transient outage | Local source |
| LOCAL-AI-CTX-01 | Observer coordinate integrity | `observerContextSchema` requires latitude/longitude as a pair and requires both for `DECLARED_COORDINATE`; positive and negative contract tests pass | `VERIFIED` | Local source only |
| LOCAL-AI-PROOF-01 | Pin-level Local AI coordinate proof | `DECLARED_COORDINATE` contexts require bounded coordinates plus a UUID `pointId`; manual submit requires a coordinate-proof asset distinct from the screenshot by both canonical SHA-256 and private reference, and persists it as `COORDINATE_PROOF` in the existing evidence table. Read projection counts all proof rows before usability validation, requires exactly one, independently binds the task `pointId` back to the Lock, returns only the proof evidence UUID, and keeps incomplete/duplicate/area evidence `UNKNOWN`. Independent blind audits found and then verified closure of the alias, malformed-duplicate and point-drift gaps. Current Node 24 contracts `232/232`, web `376/376` plus 4 skipped, lib `904/904`, worker bootstrap `3/3`, package typechecks, targeted Biome, web build and worker build pass | `VERIFIED` | Local source only; object-byte verification and runtime DB state remain `UNKNOWN` |
| LOCAL-AI-STATE-01 | Local AI result-state consistency | Public result DTO rejects `VALID` unless `taskStatus=ACCEPTED` and `validity=VALID`, and rejects `INVALID` unless an invalid-review task status and `validity=INVALID` are present; `UNKNOWN` remains available for incomplete evidence | `VERIFIED` | Local source/contracts |
| LOCAL-AI-PLAN-01 | Manual Local AI task dedupe | `planCaptureTasks` rejects duplicate scenario/contextHash/repeat dedupe keys before returning a plan, but the compatibility hash excludes `pointId`; point-distinct declared-coordinate contexts can therefore collide and make an otherwise distinct Lock unplannable | `PARTIAL` | Owner identity/versioning decision; migration compatibility may be required |
| LOCAL-AI-REVIEW-01 | Atomic, single-transition manual review | `85b91906` locks tenant-owned observation and task rows, permits only `SUBMITTED_FOR_REVIEW`, replays only the same idempotency key and payload, and commits both state changes with one audit receipt. Current Node 24 lib `904/904`, typecheck, Biome and independent verifier PASS | `VERIFIED` | Local source only; PostgreSQL concurrency remains `UNKNOWN` until disposable DB proof |
| LOCAL-MAPS-LEASE-01 | CLAIMED→SUBMITTED lease contract | Store extends `leaseExpiresAt` on submission; forward-only migration `0049_visibility_os_claimed_submit_lease.sql` now permits only a forward extension on `CLAIMED→SUBMITTED`, with a guard-shape assertion and static regression coverage. The isolated Gate12 rehearsal applied the migration chain; dedicated row-level lifecycle runner is prepared but was not executed after the stop-on-error halt | `PARTIAL` — runtime/database lifecycle proof pending | Fresh owner-authorized disposable run |
| WORKER-LOCAL-01 | Dedicated Local measurement worker boundary | Non-activating `start:local` bootstrap creates and consumes only `selena-local-measure` with concurrency 1 and retry limit 0; the default executor returns typed `OWNER_GATE_REQUIRED` with `providerCalls=0`, and built-in bootstrap tests 3/3 confirm no legacy queue/provider/credential imports. The ordinary worker no longer creates or listens to the Local queue. Docker/Railway/CI remain on the legacy entrypoint; transaction-owned executor, durable runtime state and provider registration remain absent | `PARTIAL` | Local source only; runtime owner-gated |
| ORCH-01 | Resumable orchestration state and authenticated cross-model review procedure | `ORCHESTRATION_STATE_V1_2_1.md`, `AUTHORIZATION_MATRIX_V1_2_1.md`, `CLAUDE_CODE_MAX_RUNBOOK.md`, and phase-gated `IMPLEMENTATION_PLAN_V1_2_1.md`; current Claude.ai Max authentication and latest valid Max20 session `52b8b1ae-f3cf-43a8-a859-97a198ddcfed` are recorded with evidence classes and stop conditions | `VERIFIED` — local state ledger, authorization boundaries, reusable read-only Max5/Max20 procedure, phase-gated implementation sequence, and current authentication check are committed on the feature branch; no external runtime action is implied | Local |
| PAY-01 | One-task paid canary | No approval and prerequisites incomplete | `NOT_STARTED` | Owner paid gate |
| PROD-01 | Checkout, recurring scans, production acceptance | Explicitly disabled | `NOT_STARTED` | Owner production gates |
