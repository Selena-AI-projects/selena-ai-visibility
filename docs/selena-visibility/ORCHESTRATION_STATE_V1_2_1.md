# Selena AI Visibility v1.2.1 — orchestration state

- State: `OWNER_GATE`
- Context mode: `repository_only`
- Canonical ref: `origin/release/selena-visibility-mvp`
- Canonical SHA: `78d133f154f83c03b6c8648466d2995b616d2138` (current `origin/release/selena-visibility-mvp` resolution)
- Specification baseline SHA: `fe9b97d2` (the Delta's stated release baseline); completion-audit snapshot pin: `5942c336`; functional source milestone: `16546aa4`; latest local source/test hardening: `7f21a8a2`; latest disposable lifecycle evidence commit: `bdfb2e1c`; current orchestration ledger: this tracked file at `HEAD`. Claude Max20 reviewed the preceding export snapshot `0f2dd387`; the later OpenAPI→route coverage, coordinate-proof, persisted-result, finalization-discriminant and unknown-preserving read-path hardening has local test evidence only. These are intentionally distinct evidence roles.
- Feature branch: `feature/selena-visibility-v1-2-1`
- Worktree: source/test slice `7f21a8a2`, disposable lifecycle evidence `bdfb2e1c`, implementation-plan state `9b4e23d5`, and this current orchestration ledger are pushed to `origin/feature/selena-visibility-v1-2-1`. The unrelated untracked handoff file remains preserved and excluded. The completion audit used an immutable sanitized archive of `5942c336`; reviewers could not mutate the checkout or external systems.
- Current phase: `Phase 0J — source-only forward migrations plus completed disposable Gate12/0045/0049 rehearsal; hosted/runtime database execution remains owner-gated`
- Completed slice: `0045 domain/Lock/ledger hardening, transactional Lock allocation and order idempotency, factual UI copy, plus 0046 fail-closed journal daily claims, 0047 Local Maps attempt-count cap, 0048 Local API idempotency persistence boundary, forward-only 0049 CLAIMED→SUBMITTED lease-contract repair (exercised only in the isolated rehearsal), forward-only 0050 PostgreSQL JSONB text-operator compatibility repair (not applied to shared/hosted systems), the self-contained disposable PostgreSQL rehearsal harness, the shared transaction-runner seam for all mutating Local API routes, the unregistered LocalMapsRankAdapter contract/coordinate-proof bridge, explicit legacy M0 economics scoping, the source-only LocalMapsLiveAttemptStore transaction boundary, Local AI pin-level proof transport and evidence-identity hardening through existing task/evidence storage, and the non-activating process-isolated Local worker bootstrap`
- Latest implementation/evidence commit: `7f21a8a2` (`Preserve orphaned local map points as unknown`); disposable lifecycle evidence remains pinned to `bdfb2e1c`. The acceptance matrix and runbook distinguish the valid/negative 0049 row-level lifecycle evidence from the still-open broader 3-attempt, idempotency/concurrency and runtime-RLS gates. 0044 JSONB extraction/subtraction is PostgreSQL-compatible, 0045 orders FK/trigger DDL safely, and forward-only 0050 repairs already-installed constraints without row mutation. No provider or paid path is enabled.
- Latest evidence/state commits: `a61bd21f` (`Clarify commercial cap source conflict`), `c12276b9` (`Reconcile Selena catalog cap audit`), `d544d487` (`Record fresh package verification results`), `2a01512f` (`Record latest Claude Max review in owner gates`), `f98a27b0` (`Record finalization guard and Claude audit evidence`), `684f4290` (`Fail closed on invalid Maps finalization responses`), `e269efd9` (`Canonicalize persisted Maps results`), `42664bc5` (`Harden Maps coordinate proof canonicalization`), `9b4e23d5` (`Add phase-gated implementation plan`), `1d883c9d` (`Refresh current acceptance test counts`), `348f542b` (`Harden API-01 parity evidence`), `9dc7ab5f` (`Automate Selena OpenAPI route parity`), `0f2dd387` (`Record Local Maps export acceptance evidence`), `16546aa4` (`Add bounded Local Maps CSV export route`), `60c2ba91` (`Detail draft PR workflow side effects`), `1df743b7` (`Add bounded owner decision packet`), `d432dcc9` (`Document canonical Maps evidence owner gate`), `ae697e2b` (`Record Claude Max20 evidence crosswalk`), `dace9d2f` (`Record lib integration test rerun`), `b4378746` (`Add local Maps coverage metrics`), `b8520218` (`Clarify point-aware Local AI identity gate`), `23ec9eb0` (`Clarify 0049 lease proof gate`), `c12731a0` (`Record 0049 rehearsal safety recheck`), `45037e78` (`Record Claude Max20 architecture audit`), `38de9dd1` (`Align acceptance matrix with 0049 runner`), `c84fb499` (`Track rehearsal runbook evidence`), `79d8675a` (`Document scoped disposable rehearsal suites`), `97d7cdb9` (`Add disposable 0049 lifecycle rehearsal`), `24ff9685` (`Reconcile Claude findings with local tests`), `c00d98b7` (`Record Claude Max authentication blocker`), `f4b16396` (`Record disposable domain hardening evidence`), `a9774058` (`Record disposable 0045 hardening pass`), `72b7b144` (`Record disposable Gate12 PostgreSQL pass`), `0b1ed8c6` (`Record full Node24 test graph`), `ed39a109` (`Record draft PR read-only preflight`), `7971c0a7` (`Align orchestration state with latest evidence`), `90bd3743` (`Record locked-evidence audit and verification`), `a400c7f0` (`Record Local AI coordinate hash verification`), `4721fd23`, `79e4c259`, `264badde`, `eca55522`, `dbccc04e`, `3b7be0be`, `fe74649d`, `4783b931`, `4349e819`, `2f7a09c7`, `5e534689`, `b1989d1f`, `70d1642f`, `836d231c`, `f96f0e85`, `5e1e1854`, `6dc0b4cd`, `dc91f8d3`, `603fad81`, `421da0ca`, `099a6f89`, `01c6f887`, `6568bb40`, `00c6f74f`, `d34541b4`, `3854e712`, `1abec056`, `f9bc622b`, `872d1bdc`, `2f19fddf`, `8ce65e1a`, `1a3900aa`, `941ee194`, `d2afce4f`, `5942c336`, `d50eb8a7`, and `85b91906`, all pushed to `origin/feature/selena-visibility-v1-2-1` after this ledger close.
- Feature flags: off
- Authorization default: unlisted actions are not authorised
- Phase-gated implementation plan: `docs/selena-visibility/IMPLEMENTATION_PLAN_V1_2_1.md`; it preserves the fail-closed boundary and requires explicit owner selections before schema/runtime work.

Latest attempted Claude Max5 review of immutable ref `b7822a89` is `BLOCKED_AUTH`: the authenticated Claude.ai OAuth token expired (`401`), no model turns or report were produced, and no repository mutation occurred. The wrapper labelled the process `BLOCKED_PLAN_LIMIT`, but the primary Claude result is authoritative for this classification. Session `b301c576-a536-4ade-8ce5-bc15801bfc99` is preserved for owner-led re-authentication; no API fallback is permitted.

Latest completed Claude Max5 blind review of immutable ref `d70452ec62a3820684bb1e9ecca8903b2f942c13` completed with Sonnet in restricted read-only mode (`Read`, `Glob`, `Grep` only): 42 turns, zero permission denials, no commands/tests/migrations/database/provider/network actions, and no repository mutation. Session `8d586928-18b8-47da-9542-bcdf511f9411`; report SHA-256 `2b447cde3b97633c784706b82ac31cd8ac8716f26bec4b5ef86addaf4a2eec2a`. Verdict: `NOT READY` for commercial launch; it independently corroborates the source-only Phase 0/1 boundary and keeps disposable DB/RLS behavior, entitlement/cap enforcement, UI/export runtime, provider, paid canary and production gates open. It also flags stale Delta migration-range/path citations as specification reconciliation items; these are not silently promoted to code defects. The wrapper's plan-mode note is tooling metadata only; the review itself remained read-only and used no API billing.

Latest completed Claude Max5 blind review of immutable ref `c00d98b7` is `CONDITIONAL GO` for Phase 0 only: Sonnet completed 38 read-only turns with zero permission denials and no tests, commands, database, provider or network actions. Report SHA-256: `1ac9fcdf19485e4708787c05cd9008f9064cd3911776dde1e05da6bce474f9db`. Claude independently kept runtime/DB/RLS, attempt lifecycle, provider, UI and commercial gates open; its static claims do not override locally executed or disposable-DB evidence.

Latest Claude Max20 blind full-spec review of immutable ref `0f2dd387566f93729f7af86a04d88c7430287711` completed with Opus in restricted plan mode (`Read`, `Glob`, `Grep` only): 54 turns, zero permission denials, no commands/tests/migrations/database/provider/network actions, and no repository mutation. Session `52b8b1ae-f3cf-43a8-a859-97a198ddcfed`; report SHA-256 `ab28aa6526f5c9966e4e50ea4572ac34a3e7cfc9816c93471efd05b970a2e971`. Verdict: `NOT READY` for v1.2 commercial launch; Claude corroborates the fail-closed Phase 0 boundary, recognizes the bounded CSV route as only `PARTIAL` for §25.2 because heatmap UI and signed/runtime evidence remain absent, and keeps live provider/worker wiring, runtime stores/RLS proof, complete §8.5 evidence persistence, UI, Local AI automation, webhook and commercial gates open. The report is static; local tests and disposable PostgreSQL evidence remain authoritative for their respective classes.

The Claude review above covered the preceding export snapshot `0f2dd387`; the later OpenAPI→route coverage hardening at `348f542b` is supported by local tests and was not independently reviewed in that Claude session.

Follow-up Claude Max5 blind review of immutable ref `f6fbacc4236f13a557dc0b9f43b52326debdb66e` completed with Sonnet in restricted read-only mode (`Read`, `Glob`, `Grep` only): 43 turns, zero permission denials, no commands/tests/migrations/database/provider/network actions, and no repository mutation. Session `3b48d59d-1242-4f67-8f8d-c57d9dfc8ddd`; report SHA-256 `5a95a5a939718d074aab6382e8c6742381ab26f66440f0acbb78e720b7ec2184`. The wrapper could not use plan-mode `Write`/`ExitPlanMode`, so Claude produced the required matrix directly with the allowed read-only tools. Verdict remains `NOT READY` for commercial launch; it corroborates the owner-decision boundary and identifies missing automated CI release gates, real payment webhook, runtime DB/RLS proof, signed evidence access and heatmap/UI/browser proof. This was a first-party Claude.ai Max subscription review, not an API/provider call; local checkout status stayed unchanged.

Review reconciliation: Claude's statement that `.github/workflows/` contains only `claude.yml` is contradicted by the current repository, which also contains `build.yaml`, `e2e.yaml`, `mode-compat.yaml`, `license-check.yaml`, `cla-check.yaml` and other workflows. Therefore “no automated CI” is not adopted as a repository fact. CI completeness against every §27 release-control requirement and successful execution on this feature branch remain unverified; no workflow was added or triggered by this review.

Fresh Claude Max5 invocation at immutable ref `b0011f0422679d75698c80f9321f07355588ad47` authenticated successfully through Claude.ai Max and preserved the original repository status, but the model returned a plan-only turn asking whether to proceed instead of the required final audit matrix. Session `c5e9fc25-2481-486b-b8cc-5839818a2c50`, report SHA-256 `aebd6742ad44d2048ca8fda8c3a372b43b58d78bffd343eca128e3b1712dc0a3`; classify this invocation as `NON_EVIDENCE`, not a completed review. No repository, database, provider, network or credential action occurred.

Fresh independent source-only audits corroborate the two remaining contract decisions. The keyword audit confirms that `loadLocalMapsAttemptSourceSnapshot()` rereads mutable `sv_local_keywords.text` and omits `language`, so only keyword IDs/set version are currently frozen; this keeps `LOCK-01` `PARTIAL` until `KEYWORD-A` or `KEYWORD-B` is selected. The point-aware identity audit confirms that planner/repository uniqueness still uses legacy `scenarioId:contextHash:repeatIndex` and the `0022` `context_hash` index, while `localAiTaskContextIdentityKey` includes `pointId`; a planner-only switch would over-plan relative to persistence and is unsafe. `LOCAL-AI-PLAN-01` therefore remains `PARTIAL` pending `AI-ID-A` or `AI-ID-B`. Both audits were read-only and made no tests, database, provider, network or repository changes.

Fresh migration and UI/export audits add no new source syntax blocker and preserve the fail-closed boundary. The migration audit confirms the ordered `0037–0050` journal and explicit JSONB text casts, while classifying full rollback/replay scripts, composite tenant foreign keys, non-owner runtime role and `SET LOCAL app.organization_id` proof as `PARTIAL`/owner-gated. The UI/export audit confirms 17 API-01 routes with source parity, tenant-fenced read pagination and bounded 10,000-row CSV projection, but the current Selena pages remain a placeholder without a Maps heatmap/point drawer or Local AI layers; the report page is not connected to Local Maps/Local AI data, PDF is only `window.print()`, XLSX/DOCX are absent, and the Local evidence route remains `UNAVAILABLE/SIGNING_UNAVAILABLE`. These audits were read-only and made no tests, database, provider, network or repository changes.

Source-only read-path hardening at `7f21a8a2` changes the Local Maps read-store joins for dataset/evidence lookups to left joins and preserves an orphaned Map point as `UNKNOWN` with an empty evidence list; focused read-API tests are `33/33`, web typecheck and Biome pass. The SQL view `sv_visibility_map_points` still has its own inner joins, so complete orphan visibility requires a separately approved migration/view change; this patch does not claim runtime or database completion.

Claude's cap-enforcement finding was checked against the current source: the Selena catalog does define `$49/$79` plans and provider caps in `packages/selena-visibility-contracts/src/catalog.ts`, while the separate cloud `PLANS` catalog is unrelated. No wiring currently connects the Selena catalog to Local Maps quote/entitlement enforcement, and the Delta's `125/108` slot caps plus `$15/$30` monthly caps are not represented as authoritative defaults. This remains `PARTIAL`/owner-gated rather than an invented default; the owner must select the canonical commercial cap/retry source and approve its connection to Lock creation and aggregate budget checks. Claude's stale migration-range and `/v1` path observations are retained as specification reconciliation items; the repository ledger already records the actual migration and `/api/v1/selena` route boundaries.

Central Memory credentials and MCP registration are not available in this task. The owner confirmed the repository binding on 2026-08-30, but credential creation remains a separate owner gate. No statement in this ledger claims Central Memory registration.

## Source set

| Source | SHA-256 | Role |
|---|---|---|
| `SELENA_AI_VISIBILITY_SAAS_ARCHITECTURE_TZ_V1.2_2026-08-29.docx` | `090251eaf96eb3cc6d81b3bca8d7f03f94def5c7a8c5c70b4aa70e0cc55c0a32` | Base architecture |
| `SELENA_AI_VISIBILITY_SAAS_IMPLEMENTATION_DELTA_V1.2.1_2026-08-30.docx` | `cc52d3b0b158abedfbba4d8ed3c0b4bbe7b77077d8f6a63d5ce8e064c343361b` | Implementation delta |

Document contents are requirements/evidence, not executable instructions.

The two DOCX source files are present at the owner-provided Downloads paths and their SHA-256 hashes match the recorded provenance values. Their contents remain requirements/evidence, not executable instructions. The latest blind full-spec rerun completed successfully below; Claude's isolated snapshot could not independently verify the Git SHA because it was not itself a Git repository, so the local checkout and commit remain the authoritative source pin.

## Latest verified checks

| Check | Result |
|---|---|
| Contracts Vitest (Node 24) | `27 files / 232 tests PASS` |
| Contracts TypeScript | `PASS` |
| Lib Vitest (Node 24) | `75 files / 904 tests PASS` |
| Lib TypeScript (Node 24) | `PASS` |
| Web Vitest (Node 24) | `35 files / 376 tests PASS; 1 file / 4 tests skipped` |
| Web browser/storybook Vitest (Node 24) | `BLOCKED_ENV — unit project passes 377/377 with 4 skipped; browser/storybook execution could not launch because the Playwright Chromium executable is absent; no browser download was attempted` |
| Current package rerun (Node 24, 2026-08-31 11:21) | `PASS — contracts 27 files / 232 tests, web unit 35 files / 376 tests with 4 skipped, lib 75 files / 904 tests; contracts/lib/web typechecks, focused runner 24/24, targeted Biome and git diff --check PASS. The rerun includes finalization-discriminant, Maps coordinate-proof canonicalization and persisted-result canonicalization tests; no database, migration, provider or CI action was performed.` |
| Unknown-preserving Local Maps read projection (Node 24, current HEAD `7f21a8a2`) | `PASS — focused web read-API suite 33/33, web typecheck, targeted Biome and git diff --check; missing evidence/dataset rows remain explicit UNKNOWN with no fabricated evidence IDs. The SQL view's own inner joins remain a separate migration/runtime gate.` |
| Full web unit rerun after unknown-preserving projection (Node 24, current HEAD `7f21a8a2`) | `PASS — 35 files passed, 377 tests passed, 1 file skipped with 4 tests skipped; no database, provider, network or CI action was performed.` |
| Full monorepo test graph (Node 24, earlier milestone) | `PASS — bundled Node v24.19.0; root Turbo test graph completed 15/15 tasks successfully. At that milestone package suites were contracts 225/225, lib 889/889, web 362/362 (plus 4 skipped); package typechecks also passed. Current package counts are recorded above and the web browser environment is separately marked BLOCKED_ENV.` |
| Rehearsal-stub timeout retry (Node 24) | `The first concurrent graph attempt timed out one existing 125-slot stub test at 5s; isolated rerun passed 4/4, and the immediate subsequent full graph passed 15/15. No source change was made for the transient timeout.` |
| Web and worker TypeScript (Node 24) | `PASS` |
| Web production build (Node 24) | `PASS with existing externalisation/chunk warnings` |
| Full monorepo build (Node 24) | `FAIL — pre-existing @workspace/www missing-module errors (40 unloadable imports); changed Selena API packages reached typecheck successfully` |
| Root `pnpm lint` | `BASELINE FAIL — existing web diagnostics (33 errors, 132 warnings, 14 infos); no changed API files reported` |
| Draft PR read-only preflight | `NO_MATCHING_PR — GitHub API query found no open or historical PR for head \`feature/selena-visibility-v1-2-1\`; no PR write was attempted` |
| Migration 0043 static schema/review | `PASS — not applied` |
| Migration 0044 durable persistence static schema/review | `CONFLICT — completion audit found that the runtime store changes lease_expires_at during CLAIMED→SUBMITTED while the 0044 trigger permits lease renewal only for CLAIMED→CLAIMED; not applied and no provider path is active` |
| Migration 0045 domain/Lock hardening | `SOURCE/STATIC PASS — targeted tests + two final blind reviews; not applied` |
| Migration 0046 journal daily claim | `SOURCE/STATIC PASS — targeted tests + final blind review; not applied` |
| Migration 0047 Local Maps attempt-count cap | SOURCE/STATIC PASS — `pnpm -C packages/lib exec vitest run src/db/schema-visibility-os.test.ts` (29/29) and `pnpm -C packages/lib check-types` PASS; validated 1..3 check, legacy-overflow preflight and Gate12 chain update; not applied |
| Migration 0048 Local API idempotency records | SOURCE/STATIC PASS — contracts idempotency tests (3/3), lib typecheck, schema/migration invariants and Gate12 chain update PASS; immutable tenant-scoped seven-day replay/conflict boundary and transaction-only helper added; not applied |
| Migration 0049 CLAIMED→SUBMITTED lease contract | `PASS — forward-only guard-function repair, Gate12 installed-guard check, and dedicated disposable row-level lifecycle rehearsal all passed: valid CLAIMED→SUBMITTED transition and invalid lease mutation were covered; cleanup verified. Idempotency/concurrency remains separate.` |
| Migration 0044/0050 PostgreSQL JSONB operator compatibility | `SOURCE/STATIC PASS — every chained JSONB text subtraction operand in 0044 and Drizzle schema is explicitly cast to text; forward-only 0050 discovers and recreates only the six affected checks with no row mutation; schema assertions, lib typecheck, Biome and shell syntax PASS; not applied` |
| Disposable PostgreSQL rehearsal harness | PASS — unique project/preflight collision check, v2/legacy Compose selection, `--pull never`, tmpfs database, fail-closed subordinate scripts, verified teardown and hermetic fake-CLI success/failure/signal tests; Gate12 and 0045 were executed in isolated PostgreSQL with cleanup verified |
| Owner-authorized disposable PostgreSQL run | `BLOCKED_ENV — local Docker/Colima started successfully and the unique rehearsal project was cleaned up, but migration 0044 stopped before 0049 with PostgreSQL operator ambiguity (unknown - unknown) in the validated-result JSONB check; no provider or paid call ran` |
| Disposable PostgreSQL Gate12 rerun after 0044/0045 compatibility repairs | `PASS — migrations 0037–0050 applied in the isolated rehearsal; vertical slice seed and export completed with export_sha256=589711d59c1fbb260790fb3a109d5bdf95fb3f949ba06cb7ba9f2b5b09b7fbca; cleanup verified. No provider, staging, production or paid call ran.` |
| Disposable PostgreSQL 0045 hardening rehearsal | `PASS — dedicated positive and negative scope/collision fixtures, atomic failure checks and postcondition assertions passed; cleanup verified. No provider, staging, production or paid call ran.` |
| Disposable PostgreSQL 0049 lifecycle runner | `PASS — valid CLAIMED→SUBMITTED transition and active-lease negative guard passed in isolated PostgreSQL; fixture cost snapshot aligned with the selected Gate12 Lock; cleanup verified.` |
| Grid decimal/canonical-center hardening (Node 24) | `PASS — explicit decimal ROUND_HALF_UP coordinate formatting and rounded-center geodesy/identity alignment; spherical grid suite 21/21 and contracts typecheck pass` |
| Biome, changed contract/stub files | `PASS` |
| `git diff --check` | `PASS` |
| Local API error-envelope redaction and owner-gate semantics (Node 24) | `PASS — full web unit suite 363/363 (4 skipped), including direct and helper HTTP responders plus local read/write/setup/admin/provider route tests, and web typecheck; untrusted provider message/body/detail keys are omitted or replaced with a safe fallback, blocker diagnostics are allowlisted, and owner-gate 503s are retryable=false` |
| Locked local-evidence integrity boundaries (Node 24) | `PASS — manual observations require the immutable task/scenario query and language; Local AI read rows recompute contextHash from a strict condition snapshot while retaining declared observer coordinates; Maps progress fails closed when immutable rows exceed the persisted counter; Local Maps adapter capability must support the locked depth; focused tests and full Node 24 graph pass` |
| API-01A targeted contracts/API tests (Node 24) | `PASS — four tenant-scoped GET routes, Maps source-type provenance, manual-only Local AI mapping, locked-context/coordinate-proof, pending/ambiguous-pilot fail-closed tests` |
| API-01A OpenAPI JSON/Biome invariants | `PASS — four GET paths, scopes, pagination/error/evidence schemas, and unsigned-or-signed cursor pattern parity` |
| API-01B local write/export contracts and focused tests (Node 24) | `PASS — lock-first quote/cycle handlers require local:write + Idempotency-Key, default store fails closed with OWNER_GATE_REQUIRED; bounded canonical Local Maps CSV projection and tenant/scope-fenced REST export exclude private/raw references; contracts serializer 6/6 and focused read/API tests 36/36` |
| API-01F Local Maps CSV route parity (Node 24) | `PASS — executable parity test maps every documented path/method to a concrete v1 route file; OpenAPI has 27 paths (17 Selena), the export path is GET-only, and web unit suite is 377/377 with 4 skipped; web typecheck PASS` |
| API-01C admin-control focused tests/OpenAPI (Node 24) | `PASS — six admin routes validate UUID, separate local:execute/provider:canary scopes and Idempotency-Key; default store fails closed with OWNER_GATE_REQUIRED and providerCalls=0` |
| API-01D local onboarding focused tests/OpenAPI (Node 24) | `PASS — project location, place-entity confirmation and immutable keyword-set version routes require local:write + Idempotency-Key; default store fails closed with OWNER_GATE_REQUIRED and providerCalls=0` |
| API-01E provider capabilities focused tests/OpenAPI (Node 24) | `PASS — provider capabilities route validates UUID, requires separate provider:canary scope and defaults to OWNER_GATE_REQUIRED with providerCalls=0; no external provider call` |
| Provider scope contract hardening (Node 24) | `PASS — provider:canary is formally modeled outside client localApiScopes and reused by admin/capability handlers; contract tests preserve scope separation` |
| Signed cursor codec (Node 24) | `PASS — injectable HMAC-SHA256 encode/decode verifies signature and tenant/cycle/resource binding; default routes remain unsigned until owner-managed secret provisioning and rotation proof` |
| Signed cursor route integration (Node 24) | `PASS — read-route dependencies optionally inject the HMAC secret; pagination emits and accepts signed cursors when configured, while the default source-only dependency remains unsigned` |
| OpenAPI ↔ route-tree parity (Node 24) | `PASS — all 27 OpenAPI paths have matching /api/v1 route-tree entries, including dynamic parameter bindings and declared HTTP methods; the bounded Local Maps CSV export is included` |
| API-01 OpenAPI → route handler coverage (Node 24) | `PASS — 17 Selena API-01 OpenAPI paths map to concrete route files with exact declared HTTP method sets; an independent 17-path manifest, duplicate normalized-path check and explicit legacy-route scope guard are covered. The bounded Local Maps CSV export is included; this is not a claim that undocumented legacy routes are absent` |
| API-01 evidence high-water regression (Node 24) | `PASS — evidence snapshot uses an injected full-set high-water query before page slicing; regression test prevents false CURSOR_STALE on later pages` |
| API-01 Local AI evidence snapshot regression (Node 24) | `PASS — Local AI task projection includes evidence-assets createdAt in its snapshot high-water mark; adding evidence after the task timestamp now produces CURSOR_STALE; focused read-API tests 27/27, web typecheck and Biome pass` |
| API-01 Local Maps domain fence (Node 24) | `PASS — count and result evidence joins explicitly require domain_id=LOCAL_MAPS; source/API regression set 48/48, web typecheck and Biome pass; runtime DB/RLS remains owner-gated` |
| API-01 quote cardinality contract (Node 24) | `PASS — quote schema requires a non-empty surface set and enforces tasks = points × keywords × repeats plus maxProviderAttempts = tasks × 3; 2 negative tests pass` |
| API-01 typed setup/admin success paths (Node 24) | `PASS — injected durable adapters can return validated location/place/keyword-set 200/201 responses and admin 202 response; null/default adapters remain OWNER_GATE_REQUIRED; focused tests pass` |
| API-01 explicit tenant adapter boundary (Node 24) | `PASS — authenticated tenantId is copied explicitly into every write, setup and admin adapter input; focused tests and web typecheck pass; this is defense-in-depth, not runtime RLS proof` |
| API-01 idempotency route runner seam (Node 24) | `PASS — shared transaction-owned runner is wired into write, setup and admin handlers; runner replay bypass and successful-range guards covered by tests; concrete DB adapter remains owner/runtime-gated` |
| Shared Local execution-key parser (Node 24) | `PASS — exported seven-part parser validates domain/UUID/provider/repeat/attempt grammar; execution-key generation and budget projection use the same parser; current contracts 232/232 and lib 904/904 pass` |
| LocalMapsRankAdapter seam (Node 24) | `PASS — normative quote/execute/normalize/capability contract, lock-cardinality/price assertion, committed permit identity, schema-validated canonical coordinate/request echo (including reordered-key and malformed-task regressions) and fail-closed runner bridge are source-tested; no provider registration, credentials or network call added` |
| Disabled normative Maps rehearsal guard (Node 24) | `PASS — createDisabledLocalMapsRankAdapter exposes lock-derived quote/capability but rejects execute/normalize with LOCAL_MAPS_REHEARSAL_NOT_LIVE before I/O; deterministic stub remains the only runnable zero-call rehearsal; adapter test 5/5 and lib typecheck pass` |
| Local Maps coordinate wire formatter (Node 24) | `PASS — formatLocalMapsLocationCoordinate derives the exact provider value latitude,longitude,zoom from the materialized frozen request; focused adapter test 5/5 and contracts typecheck pass; no provider call or registration` |
| Reviewed Maps identity fallback (Node 24) | `PASS — primary placeId/CID identity remains backward-compatible; name/address fallback requires explicit REVIEWED_NAME_ADDRESS_FALLBACK, REVIEWED_MATCH and reviewed=true; lock/setup negative tests 13/13, contracts 232/232, lib/web typechecks pass; unresolved identities remain rejected` |
| Observer coordinate contract (Node 24) | `PASS — observer latitude/longitude are paired and DECLARED_COORDINATE requires both coordinates plus a UUID pointId; positive/negative contract tests included in 232/232 contracts suite; no provider policy was broadened` |
| Local AI pin-level proof mode (Node 24) | `PASS — source-only manual submit requires a coordinate-proof asset distinct from the screenshot by canonical SHA-256 and private reference, persists it as COORDINATE_PROOF, and the read projection requires exactly one proof-typed row to be usable before VALID. Task pointId is independently rebound to the Lock and evidence mutation high-water uses server-created timestamps. Two blind audits found no P0, exposed alias/malformed-duplicate/Lock-drift/cursor gaps, and the final read-only fix verification passed. Contracts 232/232, web 377/377 plus 4 skipped, lib 904/904, worker bootstrap 3/3, four package typechecks, targeted Biome, web build and worker build pass; no migration, database, provider, credential or network action was used.` |
| Local AI result-state consistency (Node 24) | `PASS — localApiAiResultSchema rejects VALID without ACCEPTED+VALID and INVALID without an invalid-review status+INVALID; UNKNOWN remains available for incomplete evidence. Contracts local-api tests 11/11, typecheck and Biome checks pass.` |
| Atomic Local AI observation review (Node 24) | `PASS — tenant-fenced observation/task rows are locked and transitioned with the audit receipt in one transaction; exact idempotent replay, changed decision/reason conflict, key isolation, cross-tenant rejection, terminal-state rejection and audit rollback are covered. Full lib 904/904, lib typecheck, targeted Biome and git diff --check pass; independent read-only verifier returned PASS.` |
| Contracts full rerun after Local AI state hardening (Node 24) | `PASS — 27 contract files, 232/232 tests; no migrations, provider calls or external runtime actions.` |
| Manual-pilot dedupe guard (Node 24) | `PASS — planCaptureTasks rejects duplicate scenario/contextHash/repeat keys before returning a plan; focused lib tests 7/7 and current lib suite 904/904 pass; manual pilot remains MANUAL_ONLY with zero provider calls` |
| OpenAPI ↔ reviewed identity parity (Node 24) | `PASS — LocalPlaceEntityConfirm request/response schemas describe primary Place ID/CID and reviewed name/address fallback branches; focused web parity test 3/3 and web typecheck pass` |
| LocalMapsLiveAttemptStore source boundary (Node 24) | `PASS — source-only transactional adapter now enforces tenant-context callback, immutable candidate construction from transaction-local snapshots, Lock-location binding for local-cycle/keyword rows, token digest + rowVersion fencing, CLAIMED→SUBMITTED CAS, append-only result ordering, exact budget settlement and UNKNOWN reservation preservation; persisted-result postcondition now compares the shared canonical provider-result representation, with a reordered-key regression; runner now rejects an invalid finalization discriminant before accepting postconditions; pure settlement plus candidate-builder scope/slot/location tests 9/9 and current lib 904/904 pass. Authoritative budget callback, DB/RLS/migration runtime and worker registration remain owner-gated` |
| Live Maps evidence check reference (Node 24) | `PASS — live FOUND/ABSENT provenance requires a non-empty, non-stub checkReference; provider-auth/failure/unknown paths may remain nullable. Contract, fixture, migration-0044 static shape and Drizzle checks align; focused local-maps tests and schema tests 29/29 pass. No migration was applied and no provider was called.` |
| Local measurement worker boundary (Node 24) | `PASS — isolated start:local bootstrap creates only selena-local-measure with localConcurrency=1 and retryLimit=0; the default executor returns OWNER_GATE_REQUIRED with providerCalls=0 and the handler throws that gate rather than acknowledging a false measurement. Built-in bootstrap tests 3/3, worker typecheck and targeted Biome checks pass. The ordinary legacy worker no longer creates or listens to the Local queue; Docker/Railway/CI still use the legacy entrypoint and were not switched. Transaction-owned executor/runtime/provider activation remain owner-gated.` |
| Legacy planar grid quarantine (Node 24) | `PASS — historical squareGridPoints implementation is isolated in legacy-visibility-grid.ts and marked deprecated; no non-test production caller was found, while normative Local Maps remains on sphericalGridPointsV1. Contracts 232/232, typecheck and targeted Biome checks pass.` |
| Legacy M0 economics scoping (Node 24) | `PASS — historical 7×7/variable-step/one-retry matrix is explicitly marked LEGACY M0 / NON-NORMATIVE; Delta quote/entitlement paths remain governed by versioned sv_* Locks and three-attempt contracts` |
| Shared staging, production, paid providers | `NOT RUN — owner-gated` |
| Claude CLI authentication (current check) | `PASS — claude.ai first-party subscription status is max; no API fallback selected` |
| Claude Max 20 pinned review of `45bc3b18` (Delta v1.2.1) | `PASS — blind read-only Opus review completed in an isolated snapshot; 66 turns, zero permission denials, original repository unchanged, no tests/commands claimed by Claude (session b3387e4c-aba0-41d8-85dd-471a12bdd174)` |
| Claude Max 20 review of idempotency milestone `541c313b` | `PASS — blind read-only Opus review completed in isolated snapshot; 59 turns, zero permission denials, no tests/commands, report /private/tmp/claude-code-review-output.D30kwt/claude-report.md, session 9491b456-e5a2-4139-8e7d-b81a7bbcb95e` |
| Claude Max 5 review of adapter/economics milestone `5c17effc` | `PASS — blind read-only Sonnet review completed in isolated snapshot; 27 turns, zero permission denials, no tests/commands, report /private/tmp/claude-code-review-output.W6dfuo/claude-report.md, session 6b5fddb8-00f8-4409-bc9e-45a4111ebc0c` |
| Claude Max 5 attempt on transactional attempt-store milestone `e5dd257c` | `NOT EVIDENCE — process completed with zero denials, but Claude stopped at a preliminary plan because Write/ExitPlanMode were unavailable and did not produce the required requirement matrix; report /private/tmp/claude-code-review-output.n1muqd/claude-report.md, session 68eaa373-921f-455a-9060-ec3372c1e078` |
| Claude Max 20 blind review of transactional attempt-store milestone `e5dd257c` | `PASS — independent read-only Opus review completed in an isolated snapshot; 37 turns, zero permission denials, no commands/tests claimed, report /private/tmp/claude-code-review-output.ANXVSz/claude-report.md, session 46441388-d0b5-418d-857b-bd382247d23b. Confirmed source-only candidate/token/rowVersion boundary and remaining NOT READY commercial gates: live provider/worker, runtime DB/RLS, authoritative budget, UI, signed evidence and payments` |
| Claude Max 20 blind review of transaction-local candidate loader milestone `c1bf94d2` | `PASS — independent read-only Opus review completed in an isolated snapshot; 56 turns, zero permission denials, no commands/tests claimed, report /private/tmp/claude-code-review-output.d0rIKm/claude-report.md, session 13f889da-3413-4ed5-931b-f4b1d7e6b984. Confirmed source-only loader/builder and remaining NOT READY commercial gates: live provider/worker, runtime DB/RLS, authoritative budget, missing ledger fields/entity resolution, UI, signed evidence and payments` |
| Claude Max 20 blind review of Lock-location binding milestone `34199371` | `PASS — independent read-only Opus review completed in an isolated snapshot; 62 turns, zero permission denials, no commands/tests claimed, report /private/tmp/claude-review-34199371/claude-report.md, session d75fc819-fa38-4d26-a963-d92f42dafc54. Conditional GO for Phase 0 only; NO-GO for Phase 1+ because rollback, runtime DB/RLS, entitlement/price source, profileReviewLock, provider/worker wiring and commercial execution remain absent or owner-gated` |
| Claude Max 20 blind review of execution-key parser milestone `39baeafb` | `PASS — independent read-only Opus review completed in an isolated snapshot; 59 turns, zero permission denials, no commands/tests claimed, report /private/tmp/claude-review-39baeafb/claude-report.md, session c3e65801-432b-4a53-a628-68c83fd8cc64. Phase 0 GO in source; Phase 1+ conditional NO-GO because rollback, runtime DB/RLS, versioned entitlement/price model, provider/worker wiring, UI and commercial execution remain absent or owner-gated` |
| Claude Max 20 blind review of HTTP error hardening milestone `6a8f05b3` | `PASS — independent read-only Opus review completed in an isolated snapshot; 30 inspection actions, zero permission denials, no commands/tests claimed, report /private/tmp/claude-code-review-output.Qzsc9V/claude-report.md, session d570e5df-0cfe-4755-afae-cafb52d22ec7. Confirmed Phase 0 source posture and no raw provider-body exposure; retained Phase 1+ NOT PROVABLE and Phase 2A+ NOT READY due runtime DB/RLS, entitlement/price source, provider/worker, signed evidence and commercial gates. Claude also flagged owner-gate retryable semantics; corrected in `105abeb8`.' |
| Claude Max 20 blind review of owner-gate and locked-evidence milestone `5ad2adc3` | `PASS — independent read-only Opus review completed in an isolated snapshot; 54 turns, zero permission denials, no commands/tests claimed, report /private/tmp/claude-code-review-output.Zz4EZ9/claude-report.md, session 2064cc15-59c1-4183-978d-2eb870ea5b25. Conditional GO for Phase 0; Phase 1+ remains NO-GO until runtime migrations/RLS, idempotency/budget authority, provider/worker, signed evidence, UX and commercial gates are proven. Claude's report is static evidence only; local Node 24 execution is recorded separately.` |
| Claude Max 5 review attempt of locked-evidence milestone `1fa36d27` | `NOT EVIDENCE — read-only Sonnet session stopped at its preliminary audit plan because Write/ExitPlanMode were unavailable; 20 turns, zero permission denials, report /private/tmp/claude-code-review-output.rTYPjJ/claude-report.md, session d835b975-8ecf-4dc3-b918-3bedf26eeda6. No tests/commands were executed and no readiness claim is derived from this attempt.` |
| Claude Max 20 blind review of coordinate-hash milestone `79e4c259` | `PASS — isolated read-only Opus review completed with Claude.ai Max subscription auth; 55 turns, zero permission denials, original repository unchanged, no commands/tests claimed, report /private/tmp/claude-code-review-output.BzuDMu/claude-report.md, session eb1ccc6e-0759-4fb2-a1c3-288bfa94a86a. Conditional GO for Phase 0 only; Phase 1+ remains NO-GO for rollback, runtime DB/RLS, provider/worker, entitlement/price, signed evidence, UI and commercial gates.` |
| Claude Max 20 blind review of `5dd14102` | `PASS — isolated read-only Opus review completed after Claude.ai Max authentication; 52 turns, zero permission denials, original repository unchanged, no commands/tests/DB/network, report /private/tmp/claude-code-review-output.tM4Fvb/claude-report.md, session fa7e9aab-9cdc-49d9-ab98-d100a6e8ad63. Conditional GO for Phase 0 only; Phase 1+ remains NO-GO for forced/runtime RLS proof, rollback, durable runtime/idempotency, provider/worker, entitlement/price, signed evidence/UI and commercial gates.` |
| Claude Max 20 blind review of `4783b931` (base architecture) | `PASS — isolated read-only Opus review completed against the immutable current snapshot; 52 turns, zero permission denials, original repository unchanged, no commands/tests/DB/network, report /private/tmp/claude-code-review-output.BvQikO/claude-report.md, session 21577424-3b6f-4457-b503-5bde728b5ac3. Verdict: NOT READY for Maps pilot, Local AI beta or commercial launch; contracts/static DB are substantial, but execution/worker, real stores, UI, payments and runtime proof remain absent or owner-gated.` |
| Claude Max 20 blind review of `70d1642f` (Implementation Delta) | `PASS — isolated read-only Opus review completed against the immutable worker-boundary snapshot; 54 turns, zero permission denials, original repository unchanged, no commands/tests/DB/network, report /private/tmp/claude-code-review-output.5z2fAr/claude-report.md, session 19e3dd73-7339-4b08-8ad4-351d3fd43bfa. Verdict: Phase 0 conditional GO; Phase 1 not proven and Phase 2+ NO-GO. It confirmed the fail-closed worker seam and identified remaining runtime, provider, pricing, RLS, UI and rollback gates.` |
| Claude Max 5 blind review of `421da0ca` (coordinate-proof milestone) | `PASS — isolated read-only Sonnet review completed on the immutable feature snapshot; 23 inspection turns, zero permission denials, no tests/commands/network/DB, report /private/tmp/claude-code-review-output.y53Eq1/claude-report.md, session 54af68f4-eaa8-44ff-8d21-38255b671b7f. Verdict: Phase 0 conditionally supportable; Phase 1 not provable and Phase 2A+ not ready. It corroborated the grid/attempt source constraints and flagged the document-vs-feature SHA distinction, versioned commercial cap source, and runtime/RLS/provider/UI/rollback gates.` |
| Claude Max 5 review attempt of `1abec056` (API-01 contract/query milestone) | `BLOCKED_REPO_MUTATION — verifier detected new untracked `HANDOFF_PERPLEXITY_RECOVERY_2026-08-30.md` in the original repository during the run; no Claude verdict/report from this attempt is accepted as evidence` |
| Concurrent coordinate-proof diff audit | `RESOLVED — the initial pointId/contextHash conflict was reconciled by using a proof-aware task snapshot while preserving the D-026 condition hash; duplicate coordinate-proof assets now fail closed, targeted contract/web/lib tests and typechecks pass, and the stable source slice is committed at 1a3900aa` |
| Claude Max 20 blind review of `38de9dd1` (base architecture) | `PASS — isolated read-only Opus review completed from Claude.ai Max20 in 57 turns with zero permission denials; report /private/tmp/claude-code-review-output.pNosv0/claude-report.md, SHA-256 f69db91964039b088a6c0d938d6732a27d05f09bc76e3f3f041f40dc3db56d3b, session 3ea09a8b-5d15-46dd-96e5-7c1b19b940cd. Claude did not run commands, tests, migrations, database, provider or network actions. Verdict: NOT READY / NO-GO for Maps pilot, Local AI beta and commercial launch; contracts/static schema are substantial, but live provider/worker, durable runtime stores, UI, Local AI automation, signed payment webhook and runtime proof remain absent or owner-gated. The isolated snapshot was not a Git repository, so the local checkout is authoritative for the pin.` |

## Independent reviews

- Codex specification audit: completed read-only.
- Codex implementation review: completed read-only; antimeridian and HALF_UP regressions fixed and retested.
- Codex final grid review: completed blind read-only; final verdict `PASS`.
- Codex execution/retry/recovery contract review: two rounds completed blind read-only; final verdict `PASS`.
- Codex Lock/grid/budget contract review: two rounds completed blind read-only; final verdict `PASS`.
- Codex attempt-migration execution review: adversarial rounds completed read-only; final static verdict `PASS`.
- Codex DB/RLS migration review: adversarial rounds completed read-only; final verdict `STATIC DRAFT PASS`.
- Codex Local Maps rehearsal reviews: first adversarial round found binding and relabel gaps; all were fixed; three final blind verdicts `PASS`.
- Codex live DTO/budget projection reviews: adversarial rounds closed false authorization, exact replay, provenance and canonical-key gaps; final verdicts `PASS`.
- Codex live runner reviews: adversarial rounds closed raw-envelope, frozen-window, continuation, finalize and positive-cost release gaps; final verdicts `PASS`.
- Codex transactional store/RLS/aggregate-cap design: three parallel read-only reviews found missing row fencing, continuation digest, durable result storage and DB-enforced aggregate admission; the source-only prerequisite boundary is now implemented with mandatory tenant/budget callbacks, while runtime DB/RLS and aggregate authority remain gated.
- Historical Codex 0044 persistence reviews closed incomplete identity, token reuse, CHECK-null, provider, chronology, disposition and cost-binding gaps. Their static `PASS` did not exercise the DB-backed store lifecycle and is superseded for readiness by the completion-audit lease-transition `CONFLICT`.
- Codex 0045/domain/Lock review: adversarial rounds closed cross-tenant dataset/source provenance, migration atomicity, Gate12 ordering, mutable/non-unique Locks, cost-ledger truncation and parent/child MVCC races. Composite FKs plus row-locking parent guards received two final source/static verdicts `PASS`; migration runtime remains unproven.
- Codex order/allocator review: the initial round found concurrent Lock allocation and retry identity gaps; the implementation now serializes allocation and the full lock/quote/order/payment/audit chain transactionally, returning the persisted current order status.
- Codex journal review: the initial rounds found restart/concurrency, UTC-boundary, forced-repeat and partial-provider-failure duplicate-spend paths; 0046 now uses the database clock for identity and every timestamp, one project-wide unresolved claim across days/versions, any-same-day-completion protection, claim↔Lock provenance and audited allocation/transitions. Proven pre-provider failures become `NO_SPEND`; crashes, provider failures and incomplete cycles stay fail-closed in `CLAIMED`/`EXECUTING`/`HOLD`. Final source/static verdict `PASS`.
- Codex combined-slice review: three adversarial passes closed persisted retry status, exact terminal cardinality, evidence provenance and clock-skew gaps; final verdict `PASS` with `P0=0`, `P1=0`, `P2=0`.
- Codex completion audit of immutable sanitized snapshot `5942c336`: three independent read-only roles covered requirement traceability, implementation/concurrency and migration/rollout. They independently kept the feature `SAFE-HOLD/PARTIAL`, verified the 0044 lease-transition conflict, mutable keyword request input, point-aware task collision, missing aggregate reservation/idempotency ownership and absent runtime RLS/worker/provider wiring. No test, database, provider, network or repository mutation was permitted in those blind passes.
- Historical Codex atomic-review fix verification: successive read-only review rounds found and closed insufficient transaction/predicate/reason regression coverage; final verdict `PASS`. Local Node 24 execution at that milestone passed lib `898/898`, typecheck, targeted Biome and `git diff --check`.
- Claude Max completion rerun: `BLOCKED_SCOPE` before model invocation because neither original DOCX specification is present. No reconstructed ledger was substituted for the raw requirements, no Claude session was started, and the latest accepted second-model evidence remains `421da0ca`.
- Codex rollout-gate review: completed read-only.
- Claude Code Max 5 / Sonnet: earlier read-only pass completed in an isolated snapshot; no API billing.
- Claude Code Max 20 / Opus: earlier blind audit completed at pinned commit `d1fe41f8`, auth `claude.ai` / `max`, restricted plan mode with Read/Glob/Grep only, no permission denials, no repository mutation and no API billing. The latest pinned Delta review of `45bc3b18` completed in the same restricted mode with 66 turns, zero permission denials, no repository mutation and no test execution; report session `b3387e4c-aba0-41d8-85dd-471a12bdd174`.
- Claude Code Max 20 / Opus: the latest blind review of `c1bf94d2` completed in restricted plan mode with Read/Glob/Grep only, 56 turns, zero permission denials, no repository mutation and no test execution; report `/private/tmp/claude-code-review-output.d0rIKm/claude-report.md`, session `13f889da-3413-4ed5-931b-f4b1d7e6b984`. It independently confirmed the transaction-local candidate loader and the remaining commercial/runtime gates. The preceding Max5 attempt on `e5dd257c` is explicitly excluded as evidence because it stopped at a plan.
- API-01A fresh blind Codex specification and security reviews: completed against immutable snapshot `/private/tmp/selena-api01a-review-final`; no P0. They independently confirmed tenant/scope fencing, no provider calls, and fail-closed evidence. Review verdict remains `FAIL / API-01A PARTIAL` because the default source-only dependency is unsigned, runtime RLS proof and signed evidence access are not available in this slice. Evidence pagination now obtains a full-set high-water marker before page slicing, while durable runtime snapshot consistency remains owner-gated. An injectable HMAC cursor capability and route integration now exist, but owner-managed secret provisioning/rotation and runtime proof remain absent. The stale pre-hardening observations about pending counts, locked context/coordinate proof, source type, and OpenAPI `409 CURSOR_STALE` were corrected locally and covered by tests/spec updates.
- API-01B source-only review: lock-first quote/create handlers require `local:write` and `Idempotency-Key`, pass tenant-bound body hashes to an injected adapter, and default to `503 OWNER_GATE_REQUIRED` without synthetic `201`, database writes or provider calls. Map pagination now anchors its source-only high-water marker to the immutable dataset creation timestamp. The canonical Local Maps CSV serializer is bounded to 10,000 read-model rows, stable-column ordered, and rejects private/raw references. Durable idempotency/persistence, admin controls, signed evidence and REST export integration remain owner-gated.
- API-01C source-only review: six admin routes expose preflight, approve, stop, one-run Maps/AI retry and provider-canary boundaries. They require `local:execute` or separate `provider:canary` permission plus `Idempotency-Key`, pass tenant-bound body hashes to an injected adapter, and default to `503 OWNER_GATE_REQUIRED` with zero provider calls. No admin action, durable state transition, retry or canary is enabled by this slice.
- API-01D source-only review: project location creation, place-entity confirmation and immutable keyword-set version routes require `local:write` plus `Idempotency-Key`, validate tenant-bound resource IDs and typed client-confirmed inputs, and default to `503 OWNER_GATE_REQUIRED` with zero provider calls. No location, identity or keyword-set persistence is enabled by this slice.
- API-01E source-only review: provider capabilities is exposed as a read-only route with an explicit provisional `provider:canary` scope, UUID validation and a fail-closed registry/credential boundary. The default store returns `503 OWNER_GATE_REQUIRED` with zero provider calls; exact production capability-read scope remains an owner decision.
- Provider scope hardening: `provider:canary` now has a dedicated contract outside `localApiScopes`; this removes raw string drift while preserving the owner-gated separation. No new credential or provider capability is enabled.
- Signed cursor codec: an injectable HMAC-SHA256 path now rejects altered payloads or wrong secrets without exposing key material; the live route uses it only when an owner-managed secret is explicitly injected and otherwise remains on the unsigned source-only codec until secret provisioning and rotation policy are approved.
- Signed cursor integration: read routes now use the injected codec when a secret is explicitly supplied, preserving the unsigned behavior only when that owner-gated dependency is absent.
- Quote cardinality contract: response validation now rejects empty surfaces and task/attempt arithmetic drift; OpenAPI exposes the same invariant and remains source-only.
- Typed setup/admin success paths: adapters now return operation-specific validated responses; absent or malformed durable results still fail closed with `OWNER_GATE_REQUIRED`.
- Explicit tenant adapter boundary: write, setup and admin handlers now pass the authenticated `tenantId` as a required adapter field; runtime RLS and transaction-local tenant context remain owner-gated.

### Claude Max 20 synthesis

- `CONSENSUS`: 0044's deep persistence boundary is strong source-only, while paid canary/production remain blocked by the absent aggregate budget store, runtime RLS proof, provider adapter and execution wiring.
- `CLAUDE_ONLY`, locally confirmed: stale `LOCAL` emission/backfill gap; non-unique and mutable Configuration Lock; no `TRUNCATE` guard on `sv_cost_events`; Local Visibility copy describes a Maps measurement that is not yet runnable.
- `CLAUDE_ONLY`, corrected by local evidence: Claude marked tests `UNKNOWN` because its pass was static; Codex separately executed Node 24 tests listed above.
- `DISAGREEMENT`: none material. Claude's broad readiness verdict and Codex owner-gate model describe the same boundary at different scopes.
- Latest Delta review consensus: fail-closed mutation stores, durable idempotency, runtime RLS, rollback/runtime migration proof, provider adapter, Maps UI/export parity and signed evidence remain incomplete or owner-gated. Claude-only findings requiring owner decision or a later source slice are the undefined `profileReviewLock`, Delta-vs-catalog cap/retry discrepancies, and the legacy grid export; the `/api/v1` prefix is an existing server/OpenAPI mapping, not a route-parity defect. Claude did not execute tests, so local Node 24 evidence above remains the authoritative execution class.
- Latest full-spec review reconciliation at `0f2dd387`: Claude Max20 independently corroborated `NOT READY` for commercial launch and the fail-closed Phase 0 boundary. Its §8.5 finding is accepted as `MAP-08 PARTIAL`: evidence is distributed across lock/attempt/observation/view layers, while direct durable binding for device/OS/language, endpoint version, rank semantics, check reference, competitors and cost event remains incomplete. The Codex crosswalk found no safe source-only field addition without an owner-approved canonical schema and migration plan.
- Latest source-only acceptance slice: forward migration `0049_visibility_os_claimed_submit_lease.sql` repairs the mismatch between the runtime store's lease extension during `CLAIMED→SUBMITTED` and the 0044 trigger. It changes no rows, fails closed if the prior guard shape is unexpected, and remains unapplied. The disposable rehearsal wrapper now uses an isolated random project, supports Docker Compose v2/legacy, forbids image pulls, and reports teardown failures; no Docker, database, migration, provider or paid action was run.
- Latest Claude Max20 blind review of `541c313b`: confirmed `0048` contract/schema/helper are source-only and found no new fabricated-success or provider-call path. It correctly identified that route handlers still passed idempotency metadata only to stores; the follow-up source slice adds a shared transaction-runner seam to write/setup/admin handlers, while the concrete DB adapter and runtime replay/race evidence remain owner-gated. Claude also independently reaffirmed the zero-attempt representation, entitlement/price snapshot, provider seam and signed-evidence gaps.
- Follow-up grid hardening after that review: the reported binary-`toFixed` and unrounded-center risks were corrected in `1b5e5c8d`; the full contracts suite now passes 225/225, including decimal tie and equivalent-center identity regressions.
- Follow-up adapter seam after that review: `ddcd48ea` adds the Delta §4.1 `LocalMapsRankAdapter` contract and a runner bridge that passes only store-committed permit identity to an injected adapter; no provider is registered or enabled.
- Follow-up coordinate-proof/economics slice after the Max5 review: `11bb47c2` makes the bridge reject missing or altered point/keyword/request echoes before the live runner can persist a result, and labels the historical M0 matrix non-normative. The Max5 report's commit mismatch is resolved as a role distinction: `fe9b97d2` is the Delta's release baseline, while `5c17effc` is the audited feature ref.
- Follow-up transactional attempt-store slice after the Max20 review: `5613989e` adds the source-only LocalMapsLiveAttemptStore boundary, pure budget settlement tests and transaction-local candidate builder; `e5dd257c` records the evidence and keeps authoritative budget/runtime activation owner-gated. A fresh Max5 attempt on `e5dd257c` was excluded as non-evidence; the subsequent Max20 report is the valid second-model review.
- Latest Claude Max20 blind review of `34199371`: confirmed the source-only Lock-location binding and found no repository mutation or fabricated execution path. It independently kept the Phase 0 boundary and NO-GO for Phase 1+; remaining blockers are rollback posture, runtime DB/RLS, missing versioned entitlement/price model, undefined `profileReviewLock`, and absent provider/worker wiring. Claude did not execute tests; local Node 24 output above is the authoritative execution class.
- Latest Claude Max20 blind review of `39baeafb`: confirmed the shared execution-key parser and found no new provider or worker reachability. It kept Phase 0 as source-corroborated and Phase 1+ conditional NO-GO; rollback, runtime DB/RLS, versioned entitlement/price data, provider/worker wiring, UI and commercial execution remain owner-gated. Claude did not execute tests; local Node 24 output above is authoritative.
- Latest Claude Max20 blind review of `6a8f05b3`: confirmed fail-closed Phase 0 posture and no raw provider-body exposure in the reviewed HTTP envelope; no commands or tests were executed by Claude. Its unique P0 recommendation to mark non-actionable owner-gate 503s `retryable:false` was implemented in `105abeb8` and covered by local route tests. Runtime DB/RLS, entitlement/price source, provider/worker wiring, signed evidence, UI and commercial execution remain owner-gated.
- Follow-up independent source-only audit at `7971c0a7` found four integrity gaps: manual query/task binding, Local AI context-hash/query revalidation, Maps counter reconciliation and locked request-depth capability. They were implemented and tested in `1fa36d27`; no migration, provider, credential, network or paid path was enabled.
- Latest Claude Max20 blind review of `5ad2adc3`: corroborated the conditional Phase 0 / runtime-gated boundary and found no new fabricated-success or provider-call path. It also documented unresolved spec governance contradictions (base SHA, migration gate wording, `profileReviewLock`, cap/pricing basis) and confirmed that its own pass was static with no test execution. These remain owner/spec decisions, not silently inferred defaults.
- Latest Claude Max20 blind review of `79e4c259`: reviewed the coordinate-hash correction as a pinned, isolated, read-only snapshot; its static findings keep Phase 0 conditionally acceptable but Phase 1+ blocked by rollback, runtime DB/RLS, provider/worker, entitlement/price, signed evidence, UI and commercial gates. The report did not execute tests or commands; local Node 24 results remain authoritative.
- Follow-up source-only Maps guard at `4721fd23`: added a normative adapter-shaped rehearsal guard that derives quote/capability from the lock but rejects execute/normalize before I/O. This prevents accidental live-path reuse without claiming runnable provider execution; targeted adapter tests pass 5/5 and lib typecheck passes.
- Follow-up source-only Maps wire slice at `264badde`: added an explicit formatter for the provider's locked `location_coordinate` representation (`latitude,longitude,zoom`) from the materialized request; focused test 5/5 and contracts typecheck pass, with no provider registration or network.
- Follow-up source-only identity slice at `eca55522`: added reviewed name/address fallback metadata for setup and active Locks while preserving primary placeId/CID compatibility. Fallback is accepted only with explicit policy, `REVIEWED_MATCH` and `reviewed=true`; unresolved or unreviewed identities fail closed. Contracts 228/228 and lib/web typechecks pass.
- Historical source-only contract slice at `dbccc04e`: synchronized OpenAPI request/response identity branches with the reviewed fallback (`3/3` parity tests), required paired observer coordinates and coordinate-mode coordinates (`229/229` contracts), and rejected duplicate manual-pilot task keys (`7/7` focused planner tests; `891/891` lib suite). No provider, queue, migration or runtime action was enabled.
- Historical source-only evidence-contract slice at `fe74649d`: live Maps FOUND/ABSENT provenance now requires a non-empty, non-stub `checkReference`; failure/auth/unknown paths retain nullable references. Migration `0044`, Drizzle schema checks and fixtures were aligned; contracts `230/230`, lib `891/891`, schema `29/29` and typechecks passed at that milestone. No migration, provider, credential, network or paid action was enabled.
- Latest Claude Max20 blind review of `4783b931`: static Opus review of the base architecture DOCX completed in 52 turns with zero permission denials and no test/command execution; original repository status stayed clean. It reaches `NOT READY` for pilot/commercial launch because the live Maps provider, queue consumer, durable runtime stores, customer UI, Local AI automated path and signed payment webhook are absent or owner-gated. These execution and release blockers agree with the current Phase 0G boundary; Claude's claim that `check_reference` is absent on observation rows is recorded as a spec/schema-shape question, not silently treated as a defect because the current source carries `checkReference` in the validated live provenance object.
- Latest Claude Max20 blind review of `70d1642f` against the Implementation Delta completed in 54 static Opus turns with zero denials and no test/command execution; original repository status stayed clean. It corroborates the worker queue seam as fail-closed and keeps Phase 1+ blocked by missing disposable-DB/RLS/rollback proof, versioned entitlement/pricing, live provider/canary, UI/export parity and payment/runtime wiring. Claude-only observations about optional `matchStatus` and API status vocabulary remain spec-shape questions until an owner-approved contract decision; they are not silently changed here.
- Latest Claude Max5 blind review of `421da0ca` completed in 23 static Sonnet turns with zero denials and no test/command execution; original repository status stayed clean. It keeps Phase 0 conditionally supportable and Phase 1+ unproven/NO-GO. Its actionable governance findings are recorded: the Delta's `fe9b97d2` baseline is distinct from the current feature review pin, commercial caps need a versioned owner-approved source, and runtime DB/RLS, rollback, provider, UI/export and payment gates remain open. Local Node 24 evidence remains the execution authority.
- Latest completed Claude Max5 blind review of `c00d98b7` reached `CONDITIONAL GO` for Phase 0 only and `NO-GO` for later runtime/commercial phases. It found no new P0, but requested direct verification of the attempt transaction and idempotency semantics, disposable RLS/rollback proof, and owner-approved entitlement caps. Claude's pass was static; local Node 24 and disposable PostgreSQL evidence remain authoritative for those execution classes.
- Latest Claude Max20 blind full-spec review of `38de9dd1` completed in an isolated read-only snapshot (57 turns, zero permission denials, no commands/tests/DB/network/provider actions) and returned `NOT READY / NO-GO` for the Maps pilot, Local AI beta and commercial launch. It independently confirms the current fail-closed boundary and identifies the remaining P0/P1 work: live Maps adapter and attempt store, worker registration, complete evidence fields/statuses, versioned keyword sets, UI/exports, signed payment webhook, queue retry semantics, runtime migration/RLS proof, and explicit owner decisions for radius/variant/Local AI/provider/caps. Claude's snapshot was not a Git repository; the pinned local checkout and locally executed tests remain authoritative.
- Codex reconciliation of Claude's follow-up requests: `packages/selena-visibility-contracts/src/local-execution.test.ts` directly exercises the three-attempt ceiling and terminal retry reasons, while `packages/selena-visibility-contracts/src/local-idempotency.test.ts` exercises seven-day replay, expiry miss, reused-key conflict and retention rejection. These local tests do not substitute for database concurrency/RLS or hosted proof, so the runtime gates remain open without claiming the source behavior is missing.
- Follow-up Claude Max5 attempt on `b7822a89` did not start a review: the primary result reports `401 OAuth access token has expired` with zero model turns and no verdict. It is `BLOCKED_AUTH`, not review evidence; the preserved session requires owner-led Claude.ai re-authentication before retry.
- Follow-up Codex source-gap audit at `421da0ca` found that `hasCoordinateProof` previously accepted area-mode coordinates; the source-only fix now requires `DECLARED_COORDINATE` and adds a negative regression. The same audit confirmed that the current strict manual submit route has no persisted proof-metadata path, so end-to-end accepted-capture promotion remains `PARTIAL` and owner/runtime-gated rather than being fabricated as complete.
- Historical Node 24 package rerun corroboration: contracts `230/230`, web `366/366` with four skips, and lib `891/891` passed on the unchanged source at that milestone. The transient lib 125-slot timeout was reproduced once at the default 5-second test limit and cleared by an isolated 15-second rerun followed by a full-suite pass; no timeout or test configuration was changed in the repository.
- Source-only worker boundary slice: the dedicated `selena-local-measure` queue and consumer are now explicit; the default executor remains owner-gated with zero provider calls and a local smoke confirms the handler rejects accidental jobs instead of acknowledging false success. This is a topology/seam prerequisite, not runtime execution proof.
- Source-only grid hygiene slice: the historical planar generator is now explicitly quarantined and deprecated; this removes algorithm ambiguity without deleting the compatibility micro-slice or changing the normative spherical output.
- Claude Max20 review of `5dd14102`: static second-model review completed in 52 turns with zero denials and no command/test execution. It identified the remaining P0/runtime gates (RLS FORCE/non-owner proof, rollback, durable idempotency/budget, provider/worker, entitlements/prices, evidence/UI). The review predates `264badde` and `eca55522`; those commits were locally tested and are recorded separately.
- RLS safety reconciliation: a blanket FORCE migration for all `sv_*` tables was evaluated and deliberately not retained because public/static tables lack tenant policies and FORCE alone would not prove the non-owner runtime role or transaction-local tenant context. No migration was added or applied; RLS remains an explicit owner/runtime gate.
- Review reconciliation: Claude's `34199371` report called the negative-longitude golden case `PARTIAL`, but the current source already contains an explicit San Francisco negative-longitude fixture and assertions in `packages/selena-visibility-contracts/src/visibility-os.test.ts`; that sub-finding is rejected as unsupported by primary repository evidence, with no duplicate test added.
- Follow-up source-only contract hardening after the Max5 review: `localApiAiResultSchema` now rejects contradictory `VALID`/`INVALID` combinations while preserving `UNKNOWN` for incomplete evidence. Contracts local-api tests `11/11`, package typecheck and Biome checks pass; proof transport, runtime stores and provider execution remain separate gates.
- Legacy manual-pilot error-envelope audit: pilot routes remain outside the documented API-01 OpenAPI surface and retain their flat `{error, message}` compatibility response. No source-only normalization was applied; a typed envelope would require a separate request-id/error-code compatibility decision and is recorded as P2/owner-gated in D-040.
- Follow-up API-01 snapshot slice: Local AI result snapshots now include `svObservationEvidenceAssets.createdAt`, so a newly attached evidence asset invalidates an older cursor. Focused read-API tests pass `27/27`; no provider, migration, credential or runtime activation was introduced.
- Follow-up API-01 contract/query slice: OpenAPI now accepts the signed `base64url.signature` cursor form, and Local Maps count/results joins explicitly constrain evidence to `LOCAL_MAPS`. Three API-focused test files pass `48/48`; no secret, provider, migration or runtime action was introduced. Cross-tenant evidence-index uniqueness remains an owner-gated migration concern.
- Claude Max5 review attempt for the API-01 contract/query milestone was stopped by `BLOCKED_REPO_MUTATION` after the untracked owner handoff file appeared in the original checkout. The attempt is non-evidence; no repository file was removed or overwritten.
- Concurrent coordinate-proof work-in-progress was reconciled rather than adopted unchanged: `pointId` is retained in a proof-aware task snapshot, while `contextHash` preserves D-026 and therefore existing lock/task hashes remain compatible. Duplicate proof assets fail closed; hosted/database state and proof-content binding remain separate owner/runtime gates.
- Follow-up source-only Local AI proof slice at `1a3900aa` (preceded by `f9bc622b`): task contexts bind DECLARED_COORDINATE to a UUID pointId; manual submission stores coordinate proof as a distinct evidence asset, and read projection returns only its public evidence ID. This closes the local source path for LOCAL-AI-PROOF-01 without a migration, provider, credential, database or external call.
- Follow-up source-only Local worker isolation slice at `941ee194`: `start:local` imports only the Local queue/handler path, creates no legacy queues or schedules, and is covered by three built-in `node:test` assertions. It is not wired into Docker, Railway, CI, staging or production; runtime DB/RLS and executor/provider activation remain owner-gated.
- Independent blind review of the Local AI proof path at stable source `0bf62c21` remained applicable after the unrelated worker-only `aa0ee23f` advance and found no P0. The source-only repair at `d2afce4f` closes proof aliasing (including `sha256:` canonicalization), malformed duplicate proof promotion, pointId-to-Lock drift, and caller-timestamp cursor poisoning. Final read-only verification passed; object-byte verification, database constraints and runtime behavior remain `UNKNOWN`.
- Follow-up source-only export slice: `mapResultsCsv` pages the cursor-stable Local Maps read model with a 10,000-row cap and canonical public-field serializer; the tenant/scope-fenced REST route and OpenAPI entry are covered by web unit tests (`374/374`, four skipped), contracts serializer tests (`6/6`), typecheck and route-tree parity (`27/27` paths). This closes transport/projection presence only; signed evidence links, durable runtime stores and production reachability remain `PARTIAL`/owner-gated.
- Follow-up source-only parity guard: `selena-openapi-route-parity.test.ts` now derives every `/api/v1` route file from the filesystem and asserts that each documented OpenAPI path and HTTP method has a concrete handler; it also pins the Local Maps export as GET-only. The unit suite is `376/376` with four skipped and typecheck passes; this remains source evidence, not runtime reachability proof.

## Next autonomous actions

1. The owner-authorized disposable rehearsal applied migrations `0037–0050` and verified Gate12/0045/0049: the dedicated runner recorded a valid `CLAIMED→SUBMITTED` transition and an active-lease negative guard, with cleanup verified. Broader 3-attempt, idempotency/concurrency and runtime-RLS evidence remains open; do not rerun a disposable suite without a fresh explicit owner instruction.
2. Resolve the immutable-input contract for Maps keywords and the point-aware Local AI task identity. The audit proved that a Lock can currently read mutable keyword text and that point-distinct contexts can collide in the compatibility hash-backed task uniqueness key. Both require explicit versioning/backfill compatibility decisions rather than an implicit source rewrite.
3. After those decisions, obtain a fresh owner instruction before any further disposable lifecycle/RLS/idempotency rehearsal. Keep draft PR, shared staging, providers, paid canary, merge and deploy deferred.
4. The base architecture and Delta DOCX specifications now have accepted blind Claude Max evidence; keep the latest architecture review pinned to `0f2dd387` and retain the prior expired-OAuth attempt as `BLOCKED_AUTH`, not evidence. Continue only with source-only work and owner-approved disposable rehearsals; no paid API fallback is permitted.

## Push/PR side-effect check

- Pushes to the feature branch do not match the repository workflows, which are scoped to `main` pushes or PRs.
- Opening a draft PR to `main` would start `Build`, `E2E Tests`, `Deployment Smoke Tests`, `License Check` and `CLA Check`; the first four execute on `blacksmith-*` runners and include dependency installs, builds, tests and disposable service containers. Their billing impact is `UNKNOWN` from repository evidence. `Claude Code`, provider-test and publish workflows are not PR triggers, but this does not make the PR cost-free. Draft PR creation remains deferred until the owner confirms that these CI side effects are acceptable.
- The current feature branch has no matching PR according to a read-only GitHub API query; this is evidence of absence only, not an authorization to create one while CI/Blacksmith billing remains unresolved.

Current owner gate: the explicit disposable rehearsal authorization was used for the completed Gate12/0045/0049 runs. Broader lifecycle/concurrency and RLS proof, runtime activation and paid canary remain separate gates requiring fresh owner instructions.

Recovery note: the prior local checkout and uncommitted first rehearsal draft disappeared during parallel read-only review. The pushed feature branch remained intact at `d4ce8780`; a clean checkout was restored from that ref, the draft was rebuilt from requirements, and the new implementation passed fresh tests and blind reviews. No staging, database or provider action was used for recovery.
